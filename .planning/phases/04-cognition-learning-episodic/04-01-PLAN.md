---
phase: 04-cognition-learning-episodic
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - cua_overlay/cognition/__init__.py
  - cua_overlay/cognition/schemas.py
  - cua_overlay/learning/__init__.py
  - cua_overlay/learning/schemas.py
  - cua_overlay/state/episodic.py
  - tests/unit/cognition/test_schemas.py
  - tests/unit/learning/test_schemas.py
autonomous: true
requirements: [STATE-04, COG-01, COG-02, COG-03, COG-04, COG-05, COG-06, COG-07, COG-08, LEARN-01, LEARN-02, LEARN-03, LEARN-04, LEARN-05]

must_haves:
  truths:
    - "Cognition layer module stubs exist and can be imported with pytest.importorskip"
    - "Learning recorder module stubs exist and can be imported with pytest.importorskip"
    - "Episodic memory schema exists and TYPE_CHECKING imports resolve"
    - "All Pydantic models have frozen=True for immutability (same as Phase 1-3 pattern)"
    - "Speculation gate type enforces READ-ONLY via Literal['READ'] on N+1 candidates"
  artifacts:
    - path: "cua_overlay/cognition/__init__.py"
      provides: "Module exports for cognition layer (Planner, Grounder, Critic, etc.)"
    - path: "cua_overlay/cognition/schemas.py"
      provides: "Pydantic models: EnsembleVote, OracleOutput, PlanCandidate, PredictedState, AppleFMOutput, SpeculativeDraft"
    - path: "cua_overlay/learning/__init__.py"
      provides: "Module exports for learning recorder (LearningRecorder, ObservedAction, Recipe, RecipeSynthesizer)"
    - path: "cua_overlay/learning/schemas.py"
      provides: "Pydantic models: ObservedAction, Recipe, RecipeStep, RecipeParam, RecipePrecondition"
    - path: "cua_overlay/state/episodic.py"
      provides: "EpisodicMemory class stub with FAISS skeleton; EpisodicQuery, EpisodicHit schemas"
  key_links:
    - from: "cua_overlay/cognition/schemas.py"
      to: "tests/unit/cognition/test_schemas.py"
      via: "pytest imports → models validate"
    - from: "cua_overlay/learning/schemas.py"
      to: "cua_overlay/state/episodic.py"
      via: "Recipe objects embedded in episodic memory"
    - from: "cua_overlay/state/causal_dag.py"
      to: "cua_overlay/cognition/schemas.py"
      via: "ActionCanonical.kind Literal imported for speculation gate"
---

<objective>
Create module stubs and type contracts for Phase 4 cognition + learning + episodic subsystems. These are the foundational schemas that all downstream tasks will implement against.

Purpose: Wave 0 establishes type safety gates (esp. speculation READ-ONLY, Apple FM enum-only output) before any implementation. Tests use pytest.importorskip to skip cleanly until actual implementations ship.

Output: Module stubs + Pydantic models for Planner, Grounder, Critic, Speculator, LearningRecorder, Recipe, EpisodicMemory. All files created, no implementations yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/04-cognition-learning-episodic/04-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@cua_overlay/state/causal_dag.py
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 4 success criteria)
@.planning/REQUIREMENTS.md (STATE-04, COG-01..08, LEARN-01..05)
@cua_overlay/state/__init__.py (Phase 1 state graph pattern)
@cua_overlay/translators/base.py (Phase 2 ActionCanonical.kind Literal)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cognition module stubs + Pydantic schemas</name>
  <files>cua_overlay/cognition/__init__.py, cua_overlay/cognition/schemas.py, tests/unit/cognition/test_schemas.py</files>
  <read_first>
    - cua_overlay/state/__init__.py (module re-export pattern from Phase 1)
    - cua_overlay/translators/base.py (ActionCanonical, Literal["READ","MUTATE"] pattern)
    - .planning/phases/04-cognition-learning-episodic/04-CONTEXT.md (D-02 through D-10)
  </read_first>
  <action>
Create `cua_overlay/cognition/` sub-package mirroring Phase 1-3 structure.

1. Create `cua_overlay/cognition/__init__.py` with module-level re-exports:
   - `from .schemas import EnsembleVote, OracleOutput, PlanCandidate, PredictedState, AppleFMOutput, SpeculativeDraft`
   - Placeholder imports for future impl files (Planner, Grounder, Critic, etc.)

2. Create `cua_overlay/cognition/schemas.py` with Pydantic v2 frozen models (per Phase 1-3 precedent):
   - **AppleFMOutput** (D-02): `output: Literal["T1", "T2", "T3", "T4", "T5", "retry", "escalate", "abort"]` — hard-gated enum only, no JSON, no multi-field. Type validation uses `Literal[...]` to force mypy enforcement.
   - **PlanCandidate** (D-03): `steps: list[ActionCanonical], preconds: list[HoarePre], success_criteria: list[str], bounded: bool = True`
   - **PredictedState** (D-07): `ax_delta: dict, screenshot_phash_delta: str, expected_notifs: list[str]`
   - **EnsembleVote** (D-09): `tier: Literal["T1","T2","T3","T4","T5"], target_bbox: tuple[float, float, float, float], confidence: float, model: Literal["Opus", "GPT-5", "AppleFM"]`, frozen=True
   - **OracleOutput** (D-08): `candidates: list[ActionCanonical], ranker_model: str, top_k: int = 1` (for Critic input)
   - **SpeculativeDraft** (D-10): `action: ActionCanonical, kind: Literal["READ"]` — hard-typed to READ only (mutation gate in orchestrator level). Include `step_index: int, confidence_estimate: float`

3. Create `tests/unit/cognition/test_schemas.py`:
   - pytest.importorskip("cua_overlay.cognition") at module level — skips cleanly until impl ships
   - Test AppleFMOutput enum validation: `assert AppleFMOutput(output="T1")` passes, `AppleFMOutput(output="invalid")` raises ValidationError
   - Test SpeculativeDraft kind gate: ensure only Literal["READ"] typechecks (mypy constraint, not runtime)
   - Test all models have frozen=True via `model_config.frozen`
   - 3-4 tests, all @pytest.mark.unit
  </action>
  <verify>
    <automated>cd /Users/akeilsmith/dev/cua-maximalist && python -m pytest tests/unit/cognition/test_schemas.py -v</automated>
  </verify>
  <done>
    - cua_overlay/cognition/__init__.py exports Pydantic models
    - cua_overlay/cognition/schemas.py has AppleFMOutput, PlanCandidate, PredictedState, EnsembleVote, OracleOutput, SpeculativeDraft all frozen
    - AppleFMOutput uses Literal[...] enum hard-gate (P6)
    - SpeculativeDraft.kind hard-typed Literal["READ"] (P22)
    - Test file runs, skips cleanly with pytest.importorskip
  </done>
</task>

<task type="auto">
  <name>Task 2: Create learning module stubs + Recipe/ObservedAction schemas</name>
  <files>cua_overlay/learning/__init__.py, cua_overlay/learning/schemas.py, tests/unit/learning/test_schemas.py</files>
  <read_first>
    - cua_overlay/state/__init__.py (module re-export pattern from Phase 1)
    - .planning/phases/04-cognition-learning-episodic/04-CONTEXT.md (D-11 through D-17)
    - cua_overlay/translators/base.py (ActionCanonical import, action schema)
  </read_first>
  <action>
Create `cua_overlay/learning/` sub-package for CGEvent tap recorder + Recipe synthesis.

1. Create `cua_overlay/learning/__init__.py` with module-level re-exports:
   - `from .schemas import ObservedAction, Recipe, RecipeStep, RecipeParam, RecipePrecondition`
   - Placeholder imports for recorder.py, recipe_synth.py, coalesce.py (future tasks)

2. Create `cua_overlay/learning/schemas.py` with Pydantic v2 frozen models:
   - **ObservedAction** (D-15): `step_idx: int, action: ActionCanonical, user_gesture_type: Literal["keystroke", "click", "scroll"], timestamp: float, success: bool, ax_delta: dict | None = None`
   - **RecipeParam** (D-16): `name: str, description: str, type: Literal["str", "int", "bbox", "element"]`
   - **RecipePrecondition** (D-16): `expression: str, expected_value: Any, confidence: float`
   - **RecipeStep** (D-16): `idx: int, action: ActionCanonical, preconditions: list[RecipePrecondition], on_failure: list[str]` (recovery hints per step)
   - **Recipe** (D-16): `name: str, app_bundle_id: str, params: list[RecipeParam], preconditions: list[RecipePrecondition], steps: list[RecipeStep], success_criteria: list[str], created_ts: float`

3. Create `tests/unit/learning/test_schemas.py`:
   - pytest.importorskip("cua_overlay.learning") at module level
   - Test Recipe creation with valid nested step structure
   - Test ObservedAction.action field must be ActionCanonical (import from causal_dag)
   - Test all models frozen=True
   - 3-4 tests, all @pytest.mark.unit
  </action>
  <verify>
    <automated>cd /Users/akeilsmith/dev/cua-maximalist && python -m pytest tests/unit/learning/test_schemas.py -v</automated>
  </verify>
  <done>
    - cua_overlay/learning/__init__.py exports Recipe, ObservedAction, RecipeStep, RecipeParam, RecipePrecondition
    - cua_overlay/learning/schemas.py has all frozen models
    - Recipe.steps is list[RecipeStep] with on_failure per step (D-16)
    - Test file runs, skips cleanly with pytest.importorskip
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend state/episodic.py + create EpisodicMemory schema stubs</name>
  <files>cua_overlay/state/episodic.py, tests/unit/state/test_episodic.py</files>
  <read_first>
    - cua_overlay/state/__init__.py (import pattern)
    - cua_overlay/state/fingerprint.py (state_fingerprint logic)
    - .planning/phases/04-cognition-learning-episodic/04-CONTEXT.md (D-18, D-19, D-20, STATE-04)
    - .planning/research/STACK.md (faiss-cpu 1.13.2 choice locked)
  </read_first>
  <action>
Extend `cua_overlay/state/episodic.py` to define schemas for FAISS integration (implementation in Wave 4).

1. Add to `cua_overlay/state/episodic.py` (append to existing stub if present, or create fresh):
   - **EpisodicQuery** (D-20): `app_bundle_id: str, task_class: str, state_fingerprint: str, query_embedding: list[float], top_k: int = 3`
   - **EpisodicHit** (D-20): `recipe: Recipe, similarity: float, embedding_source_text: str, success_count: int, failure_count: int, quarantined: bool = False` (D-19 track success/failure; quarantine on >2 failures)
   - **EpisodicMemory** (skeleton, D-18): class stub with docstring referencing FAISS IndexFlatL2, faiss-cpu 1.13.2, local-only at ~/.cua/episodic.faiss

2. Export from `cua_overlay/state/__init__.py`:
   - Add `from .episodic import EpisodicMemory, EpisodicQuery, EpisodicHit`

3. Create `tests/unit/state/test_episodic.py`:
   - pytest.importorskip("cua_overlay.state.episodic") at module level
   - Test EpisodicHit construction with Recipe + metadata
   - Test EpisodicQuery creation with app_bundle_id, task_class, state_fingerprint
   - Test frozen models
   - 2-3 tests, all @pytest.mark.unit
  </action>
  <verify>
    <automated>cd /Users/akeilsmith/dev/cua-maximalist && python -m pytest tests/unit/state/test_episodic.py -v</automated>
  </verify>
  <done>
    - cua_overlay/state/episodic.py has EpisodicMemory, EpisodicQuery, EpisodicHit schemas
    - EpisodicHit tracks success_count, failure_count, quarantined flag (D-19 P22)
    - cua_overlay/state/__init__.py re-exports these
    - Test file runs, skips cleanly with pytest.importorskip
    - Comment in code references FAISS IndexFlatL2 + ~/thinker/vault/research/ locked architecture
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppleFMOutput enum validation | Untrusted LLM output (Apple FM) crosses here; must hard-validate against Literal enum |
| SpeculativeDraft.kind type gate | Type system enforces READ-only; Python runtime cannot enforce at exec time (Literal hint only) |
| Recipe ingestion | Untrusted user-recorded recipe flows to episodic memory; must validate preconditions + step count before indexing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | AppleFMOutput enum | mitigate | Pydantic v2 hard-validation at schema level; ValidationError raised on mismatch (per D-02) |
| T-4-02 | Information Disclosure | Recipe in episodic.faiss | accept | Local-only FAISS file at ~/.cua/episodic.faiss; no network transmission; user trusts their own machine |
| T-4-03 | Denial of Service | EpisodicMemory query | accept | Top-k=3 bounded; FAISS IndexFlatL2 scales to 100k vectors; no unbounded loops |
| T-4-04 | Logic Bomb | SpeculativeDraft mutation gate | mitigate | Type system enforces Literal["READ"] at mypy time; orchestrator mutation gate blocks N+1 MUTATE at runtime (Wave 2 enforcement) |

</threat_model>

<verification>
Schema imports work across all test files. All Pydantic models validate correctly. Type annotations resolve without errors.

Run:
```bash
cd /Users/akeilsmith/dev/cua-maximalist
python -m mypy cua_overlay/cognition/schemas.py cua_overlay/learning/schemas.py cua_overlay/state/episodic.py --strict 2>&1 | head -20
python -m pytest tests/unit/cognition/test_schemas.py tests/unit/learning/test_schemas.py tests/unit/state/test_episodic.py -v
```

Expected: All tests pass or skip cleanly. mypy exits 0 (strict mode).
</verification>

<success_criteria>
- All module __init__.py files created and export correct schemas
- Pydantic models all have frozen=True
- AppleFMOutput uses Literal[...] enum (hard-gate P6)
- SpeculativeDraft.kind is Literal["READ"] (hard-gate P22)
- EpisodicHit tracks success_count, failure_count, quarantined (D-19)
- Tests use pytest.importorskip; skip cleanly until impls ship
- mypy strict mode passes
- No implementation code — schemas only
</success_criteria>

<output>
After completion, create `.planning/phases/04-cognition-learning-episodic/04-01-SUMMARY.md` documenting:
- All schema files created + paths
- Pydantic hard-gates for P6, P22
- Test skip pattern used (pytest.importorskip)
- Next wave (Wave 1) can now import these schemas without implementation
</output>
