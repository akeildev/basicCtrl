---
phase: 04-cognition-learning-episodic
plan: 07
type: execute
wave: 5
depends_on: [04-01, 04-02, 04-03, 04-04, 04-06]
files_modified:
  - basicctrl/recovery/branches/b3_world_replan.py
  - basicctrl/recovery/branches/b4_planner_replan.py
  - basicctrl/profile/classifier.py
  - tests/unit/recovery/branches/test_b3_world_replan.py
  - tests/unit/recovery/branches/test_b4_planner_replan.py
autonomous: true
requirements: [COG-01, COG-06, COG-04, COG-08]

must_haves:
  truths:
    - "B3 (world-model replan) calls WorldModelPredictor.predict() + Planner.replan(); replaces stub"
    - "B4 (planner replan) calls Critic.rank_candidates() to pick best replan; replaces stub"
    - "AppProfile gains cognition_capable field; capability probe at session start"
    - "B3/B4 still respect Phase 3 contracts: try_claim, cancel_event check, max_cycles=2 gate"
  artifacts:
    - path: "basicctrl/recovery/branches/b3_world_replan.py"
      provides: "B3RecoveryBranch class; calls world_model.predict() + planner.replan()"
    - path: "basicctrl/recovery/branches/b4_planner_replan.py"
      provides: "B4RecoveryBranch class; calls critic.rank_candidates() for best replan"
    - path: "basicctrl/profile/classifier.py"
      provides: "AppProfile.cognition_capable field + capability probe"
    - path: "tests/unit/recovery/branches/test_b3_world_replan.py"
      provides: "Unit tests: B3 calls predictor + planner, respects Phase 3 contracts"
    - path: "tests/unit/recovery/branches/test_b4_planner_replan.py"
      provides: "Unit tests: B4 calls Critic, returns top-1 replan"
  key_links:
    - from: "basicctrl/recovery/branches/b3_world_replan.py"
      to: "basicctrl/cognition/world_model.py, planner.py"
      via: "B3 calls both for pre-execution prediction + replanning"
    - from: "basicctrl/recovery/branches/b4_planner_replan.py"
      to: "basicctrl/cognition/critic.py"
      via: "B4 calls Critic.rank_candidates() for best replan"
    - from: "basicctrl/profile/classifier.py"
      to: "basicctrl/cognition/"
      via: "AppProfile.cognition_capable probes if local models available"
---

<objective>
Replace Phase 3 stub branches B3 and B4 with real implementations wired into cognition layer. B3 calls world-model predictor + planner. B4 calls Critic to rank planner outputs. Also add AppProfile.cognition_capable field for graceful degradation if local models unavailable.

Purpose: Close the loop between recovery (Phase 3) and cognition (Phase 4). Recovery branches now have real decision-making capability.

Output: B3 and B4 implementations + AppProfile capability probe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/04-cognition-learning-episodic/04-CONTEXT.md (D-22, D-23, D-24)
@basicctrl/recovery/orchestrator.py (B3/B4 contract: try_claim, cancel_event check, max_cycles gate)
</execution_context>

<context>
@basicctrl/recovery/branches/b3_world_replan_stub.py (existing stub to replace)
@basicctrl/recovery/branches/b4_planner_reqry_stub.py (existing stub to replace)
@basicctrl/cognition/world_model.py (Wave 1 implementation)
@basicctrl/cognition/planner.py (Wave 1 implementation)
@basicctrl/cognition/critic.py (Wave 2 implementation)
@basicctrl/profile/classifier.py (AppProfile location)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace B3 stub with real world-model replan branch</name>
  <files>basicctrl/recovery/branches/b3_world_replan.py, tests/unit/recovery/branches/test_b3_world_replan.py</files>
  <read_first>
    - basicctrl/recovery/branches/b3_world_replan_stub.py (existing contract to preserve)
    - .planning/phases/04-cognition-learning-episodic/04-CONTEXT.md (D-22, D-24)
    - basicctrl/cognition/world_model.py (WorldModelPredictor.predict() signature)
    - basicctrl/cognition/planner.py (Planner.replan() signature)
  </read_first>
  <action>
Replace `basicctrl/recovery/branches/b3_world_replan.py` stub with real implementation per D-22, D-24.

1. Create B3RecoveryBranch class:
   ```python
   class B3RecoveryBranch(RecoveryBranch):
       async def try_claim(self, failure_ctx: FailureCtx, state: StateGraph) -> bool:
           """Per Phase 3 contract: atomic claim or return False if already claimed."""
       
       async def fire(self, failure_ctx: FailureCtx, current_state: StateGraph) -> ActionCanonical | None:
           """
           D-22: Call world_model.predict() + planner.replan().
           Per D-24: still respects try_claim + cancel_event check + Phase 3 contracts.
           
           1. Check cancel_event (if orchestrator cancelled, return None)
           2. Call world_model.predict(failed_action, current_state) → PredictedState
           3. Use predicted post-state to guide planner.replan()
           4. Return replanned ActionCanonical or None on failure
           """
           
           if self.cancel_event.is_set():
               return None
           
           predicted = await world_model.predict(failure_ctx.action, current_state)
           replanned = await planner.replan(goal, current_state, predicted_state=predicted)
           return replanned.steps[0] if replanned else None
   ```

2. Create tests:
   - `tests/unit/recovery/branches/test_b3_world_replan.py`:
     - Test 1: Mock world_model + planner, B3.fire() returns replanned ActionCanonical
     - Test 2: cancel_event check — B3.fire() returns None if cancel_event set before fire
     - Test 3: Phase 3 contract — try_claim returns False on second claim
     - @pytest.mark.unit for all (mocked cognition layer)
  </action>
  <verify>
    <automated>cd /Users/akeilsmith/dev/basicCtrl && python -m pytest tests/unit/recovery/branches/test_b3_world_replan.py -v</automated>
  </verify>
  <done>
    - B3 stub replaced with real implementation calling world_model + planner
    - D-24 Phase 3 contracts respected (try_claim, cancel_event, orchestrator gates)
    - Tests verify B3 behavior + contract adherence
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace B4 stub + add AppProfile.cognition_capable probe</name>
  <files>basicctrl/recovery/branches/b4_planner_replan.py, basicctrl/profile/classifier.py, tests/unit/recovery/branches/test_b4_planner_replan.py</files>
  <read_first>
    - basicctrl/recovery/branches/b4_planner_reqry_stub.py (existing contract)
    - .planning/phases/04-cognition-learning-episodic/04-CONTEXT.md (D-23, D-24, D-31)
    - basicctrl/cognition/critic.py (Critic.rank_candidates() signature)
    - basicctrl/profile/classifier.py (AppProfile structure)
  </read_first>
  <action>
Replace B4 stub with real implementation + add AppProfile.cognition_capable per D-23, D-24, D-31.

1. Create B4RecoveryBranch class:
   ```python
   class B4RecoveryBranch(RecoveryBranch):
       async def try_claim(self, failure_ctx: FailureCtx, state: StateGraph) -> bool:
           """Per Phase 3 contract."""
       
       async def fire(self, failure_ctx: FailureCtx, current_state: StateGraph) -> ActionCanonical | None:
           """
           D-23: Call critic.rank_candidates() to pick best replan from N candidates.
           Per D-24: still respects Phase 3 contracts.
           
           1. Check cancel_event
           2. Generate N candidate replans (e.g., call planner N times with different prompts)
           3. Call critic.rank_candidates(candidates, criterion="planner_replan") → top-1
           4. Return ranked ActionCanonical or None
           """
   ```

2. Extend AppProfile in `basicctrl/profile/classifier.py`:
   - Add field: `cognition_capable: bool | None = None`
   - During capability probe (Phase 1 probe runs at session start), check:
     - Apple FM SDK available + macOS 26+ + Apple Intelligence enabled?
     - mlx-vlm installable + UI-TARS-1.5-7B model loadable?
     - FAISS + sentence-transformers available?
   - Set `cognition_capable = True` if all available, `False` if any missing
   - Graceful degradation: if cognition_capable=False, disable B3/B4 recovery branches + ensemble voting

3. Create tests:
   - `tests/unit/recovery/branches/test_b4_planner_replan.py`:
     - Test 1: Mock planner + critic, B4.fire() returns top-1 ranked replan
     - Test 2: cancel_event check — B4.fire() returns None if cancelled
     - Test 3: Phase 3 contract — try_claim
     - @pytest.mark.unit
   - `tests/unit/profile/test_classifier.py` (extend existing):
     - Test 1: cognition_capable = True when all models available (mocked)
     - Test 2: cognition_capable = False when mlx-vlm unavailable
     - Test 3: AppProfile persists cognition_capable across session restart (cache check)
  </action>
  <verify>
    <automated>cd /Users/akeilsmith/dev/basicCtrl && python -m pytest tests/unit/recovery/branches/test_b4_planner_replan.py tests/unit/profile/test_classifier.py -v</automated>
  </verify>
  <done>
    - B4 stub replaced with real implementation calling Critic.rank_candidates()
    - D-24 Phase 3 contracts respected
    - AppProfile.cognition_capable field added + capability probe
    - Graceful degradation: disable B3/B4/ensemble if cognition_capable=False
    - Tests verify B4 behavior + capability probe logic
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cognition layer availability | If models unavailable, recovery branches degrade gracefully (cognition_capable=False) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Availability | Missing models | accept | AppProfile.cognition_capable probes at session start; B3/B4 skip if unavailable |

</threat_model>

<verification>
Run B3/B4 + AppProfile tests. Verify Phase 3 contract adherence + graceful degradation.

```bash
cd /Users/akeilsmith/dev/basicCtrl
python -m pytest tests/unit/recovery/branches/test_b3_world_replan.py tests/unit/recovery/branches/test_b4_planner_replan.py tests/unit/profile/test_classifier.py -v
```

Expected: All tests pass. B3/B4 contract checks pass.
</verification>

<success_criteria>
- B3 calls world_model.predict() + planner.replan(); respects Phase 3 contracts
- B4 calls Critic.rank_candidates(); respects Phase 3 contracts
- AppProfile.cognition_capable field added + capability probe at session start
- Graceful degradation: B3/B4/ensemble skip if cognition_capable=False
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-cognition-learning-episodic/04-07-SUMMARY.md` documenting:
- B3/B4 real implementations
- AppProfile.cognition_capable integration
- Graceful degradation strategy
- Ready for Wave 5 (integration tests + demo)
</output>
