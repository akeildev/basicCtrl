---
phase: 05-visualizer-full-transparency
plan: 07
type: execute
wave: 5
depends_on: [01, 06]
files_modified:
  - basicctrl/replay/counterfactual.py
  - libs/cua-driver/App/CounterfactualRenderer.swift
autonomous: true
requirements: [OBS-05]
user_setup: []

must_haves:
  truths:
    - "Counterfactual renderer overlays alternate recovery branch states"
    - "Dashed purple path shows 'what if' divergence in 3D timeline"
    - "Post-divergence states rendered semi-transparent (opacity toggle)"

---

<objective>
Implement counterfactual replay showing "what if recovery branch B had won?" via semi-transparent overlays and dashed path on timeline.

Purpose: OBS-05 (counterfactual visualization).

Output: counterfactual.py + CounterfactualRenderer.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-visualizer-full-transparency/05-UI-SPEC.md#counterfactual-replay
@.planning/phases/05-visualizer-full-transparency/05-RESEARCH.md#session-diff
</context>

<tasks>

<task type="auto">
  <name>Task 1: Counterfactual state renderer (Python data model)</name>
  <files>basicctrl/replay/counterfactual.py</files>
  <action>
Create basicctrl/replay/counterfactual.py:

```python
"""Counterfactual replay — alternate branch state reconstruction."""
from basicctrl.replay.engine import ReplayEngine


class CounterfactualRenderer:
    """Renders alternate recovery branch outcomes."""
    
    def __init__(self, engine: ReplayEngine):
        self.engine = engine
    
    def get_alternate_state(self, step_idx: int, alternate_branch: str) -> dict:
        """Reconstruct state if branch B had won instead of actual winner."""
        # Load branch outcomes from Phase 3 recovery logs
        # Return post-divergence StateNode with branch B actions applied
        return {}
    
    def get_divergence_point(self) -> int:
        """Find first step where multiple branches existed."""
        for i, action in enumerate(self.engine.actions):
            if action.get("recovery_branches"):
                return i
        return 0
    
    def render_dashed_path(self, branch: str) -> list[tuple[float, float]]:
        """Generate dashed timeline path for alternate branch."""
        return []  # Will be consumed by Swift Canvas
```

Pattern: Data model. Swift Canvas renders dashed path + semi-transparent overlays.
  </action>
  <verify>
    <automated>
cd /Users/akeilsmith/dev/basicCtrl && python -c "
from basicctrl.replay.counterfactual import CounterfactualRenderer
print('✓ CounterfactualRenderer imports')
"
    </automated>
  </verify>
  <done>
    - CounterfactualRenderer class with branch state reconstruction
    - get_divergence_point() finds alternate outcomes
    - render_dashed_path() generates visualization data
  </done>
</task>

<task type="auto">
  <name>Task 2: SwiftUI counterfactual overlay (Canvas rendering)</name>
  <files>libs/cua-driver/App/CounterfactualRenderer.swift</files>
  <action>
Create libs/cua-driver/App/CounterfactualRenderer.swift:

```swift
import SwiftUI

struct CounterfactualOverlayView: View {
    let branchName: String
    let dashedPath: [CGPoint]
    let opacity: Double = 0.4
    
    var body: some View {
        Canvas { context, size in
            // Draw dashed line for alternate path
            var path = Path()
            for (i, point) in dashedPath.enumerated() {
                if i == 0 {
                    path.move(to: point)
                } else {
                    path.addLine(to: point)
                }
            }
            
            // Dashed stroke in purple
            context.stroke(
                path,
                with: .color(.purple.opacity(opacity)),
                lineWidth: 2.0,
                dash: [4, 4]  // Dashed pattern
            )
            
            // Overlay label at divergence point
            let text = Text("Branch \(branchName)")
                .font(.system(size: 11, weight: .medium))
                .foregroundColor(.purple)
            
            if let first = dashedPath.first {
                context.draw(text, at: first, anchor: .topLeading)
            }
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
```

Pattern: Canvas renders dashed path + label. Opacity toggle in parent view.
  </action>
  <verify>
    <automated>
cd /Users/akeilsmith/dev/basicCtrl && swift build 2>&1 | grep -c "error:" || echo "✓ CounterfactualRenderer compiles"
    </automated>
  </verify>
  <done>
    - CounterfactualOverlayView renders dashed path in purple
    - Opacity configurable
    - Label shows branch name
  </done>
</task>

</tasks>

<success_criteria>
- [ ] CounterfactualRenderer.get_alternate_state() reconstructs branch outcomes
- [ ] CounterfactualOverlayView renders dashed purple path
- [ ] Test: `pytest tests/test_replay.py::test_counterfactual_dashed_path_snapshot -x`
</success_criteria>

<output>
After completion, create `.planning/phases/05-visualizer-full-transparency/05-07-SUMMARY.md`
</output>
