---
phase: 03-recovery-cache-write-back
plan: 01
type: auto
wave: 0
depends_on: []
files_modified:
  - tests/unit/recovery/conftest.py
  - tests/unit/recovery/__init__.py
autonomous: true
requirements: [HEAL-01]
user_setup: []

must_haves:
  truths:
    - "FailureClass enum has 6 typed variants matching architecture (PERCEPTUAL / COGNITIVE / ACTUATION / ENVIRONMENTAL / RESOURCE / LOOP)"
    - "Unit tests for classifier module exist as Wave-0 stubs (pytest.importorskip pattern)"
  artifacts:
    - path: basicctrl/recovery/__init__.py
      provides: Module initialization, re-exports for classifier, branches, orchestrator
    - path: tests/unit/recovery/conftest.py
      provides: Fixture scaffolding for all recovery tests (failure_ctx_factory, verifier_mock, session_writer_mock)
  key_links:
    - from: "basicctrl/recovery/"
      to: "basicctrl/verifier/"
      via: "imports HoarePost, Aggregator"
    - from: "tests/unit/recovery/conftest.py"
      to: "basicctrl/persist/session_writer.py"
      via: "pytest fixture mocking SessionWriter.append_action_log"

---

# Phase 03 Plan 01: Recovery Module Scaffold + Test Fixtures

**Create module structure for recovery subsystem; define Wave-0 test scaffolding with importorskip pattern so tests collect cleanly until real modules ship.**

## Objective

Establish recovery module organization at `basicctrl/recovery/` with clear separation of concerns (classifier, branches, orchestrator, circuit breaker, heal events) and provide shared pytest fixtures for recovery tests. This unblocks parallel Wave 1 + Wave 2 task work.

Purpose: Prevent test collection failures when real modules ship; provide dependency injection mocks for unit tests; establish pattern from Phase 1/2 patterns.

Output: `basicctrl/recovery/` module tree + conftest fixtures + Wave-0 test stubs that skip cleanly.

## Execution Context

@.planning/phases/03-recovery-cache-write-back/03-CONTEXT.md
@.planning/REQUIREMENTS.md (HEAL-01..05, CACHE-01..03)
@.planning/research/ARCHITECTURE.md
@basicctrl/recovery/ (empty yet)
@tests/unit/recovery/ (empty yet)

## Tasks

<task type="auto">
  <name>Task 1: Create recovery module scaffold (basicctrl/recovery/__init__.py)</name>
  <files>basicctrl/recovery/__init__.py</files>
  <action>Create module init with docstring explaining subsystem: "Recovery orchestration after verification failure. Classifies failures, dispatches 5 parallel branches, takes first-verified result, emits heal events, writes back to cassette atomically."

  Module structure (docstring only, no implementation yet):
  - `classifier.py` — 6-class FailureClass enum + dispatch table
  - `branches/` — b1_rescroll.py, b2_ocr_reground.py, b3_world_replan_stub.py, b4_planner_reqry_stub.py, b5_applescript.py
  - `orchestrator.py` — RecoveryOrchestrator + BranchOrchestrator + first_verified wrapper
  - `circuit_breaker.py` — per-(bundle_id, target_key) counter, trip on 3 consecutive failures
  - `heal_event.py` — HealEvent Pydantic model, emitters

  For now, just the module docstring. Placeholder imports will be added by Wave 1/2 tasks.
  </action>
  <verify>File created with module docstring; `python -c "import basicctrl.recovery"` succeeds</verify>
  <done>basicctrl/recovery/__init__.py exists with docstring describing subsystem structure</done>
</task>

<task type="auto">
  <name>Task 2: Create pytest fixtures for recovery unit tests (tests/unit/recovery/conftest.py)</name>
  <files>tests/unit/recovery/conftest.py</files>
  <action>Create conftest.py with shared fixtures for all recovery tests. Define:

  1. `failure_ctx_factory` — fixture returning a factory function that builds FailureCtx objects (once FailureCtx is defined in Wave 1, tests import it; for now use TypedDict placeholder).
     - Parameters: bundle_id, target_key, failure_class (default PERCEPTUAL), confidence (default 0.3), last_error (default "test error")
     - Returns: dict or Pydantic model matching Phase 3 FailureCtx contract

  2. `verifier_mock` — AsyncMock for Aggregator.verify, returns HoarePost with verified=False by default (simulates failure condition)
     - Can be configured with `verifier_mock.return_value = HoarePost(verified=True, ...)` per test

  3. `session_writer_mock` — AsyncMock for SessionWriter.append_action_log
     - Tests can assert calls: `session_writer_mock.append_action_log.assert_called`

  4. `idempotency_store_mock` — AsyncMock for IdempotencyTokenStore from Phase 2
     - Tests can configure `.try_claim()` return value

  5. `axmgr_mock` — AsyncMock for AXObserverManager.expect (Phase 1)
     - Default: succeeds with no error

  Per Phase 1/2 pattern, use `@pytest.fixture` + `AsyncMock` from `unittest.mock`.
  Include module docstring: "Fixtures for recovery unit tests. All fixtures mock Phase 1/2 dependencies."
  </action>
  <verify>`python -m pytest tests/unit/recovery/conftest.py --collect-only` shows 5 fixtures collected</verify>
  <done>tests/unit/recovery/conftest.py exists with all 5 fixtures defined and importable</done>
</task>

<task type="auto">
  <name>Task 3: Create test package structure with Wave-0 stubs (tests/unit/recovery/__init__.py)</name>
  <files>tests/unit/recovery/__init__.py</files>
  <action>Create tests/unit/recovery/__init__.py (can be empty or minimal docstring).

  Create Wave-0 stub test files that follow Phase 2 pattern:
  - tests/unit/recovery/test_classifier.py — `pytest.importorskip('basicctrl.recovery.classifier')` at module top
  - tests/unit/recovery/test_branches.py — `pytest.importorskip('basicctrl.recovery.branches')`
  - tests/unit/recovery/test_orchestrator.py — `pytest.importorskip('basicctrl.recovery.orchestrator')`
  - tests/unit/recovery/test_circuit_breaker.py — `pytest.importorskip('basicctrl.recovery.circuit_breaker')`
  - tests/unit/recovery/test_heal_event.py — `pytest.importorskip('basicctrl.recovery.heal_event')`

  Each stub file has:
  - Module docstring (e.g., "Tests for recovery classifier. Skips until classifier.py ships.")
  - `pytest.importorskip()` call on line 1 (not in conftest — per Phase 2, each test file guards its own import)
  - Single placeholder test: `def test_placeholder_skip_until_module_ships(): pass`

  No actual tests yet — Wave 1/2 tasks will replace stubs with real tests.
  </action>
  <verify>`pytest tests/unit/recovery/ --collect-only -q` shows all 5 test files collected and skipped (not errored)</verify>
  <done>tests/unit/recovery/ exists with __init__.py + 5 Wave-0 stub test files; pytest collects without error</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None at scaffold stage — Wave 1 will define contracts.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information | silent heal masking regressions | mitigate | Wave 1: HealEvent emitted for every heal; rate budget pauses auto-heal at >5% |
| T-3-02 | Tampering | cassette write-back loop | mitigate | Wave 4: stable-tier gate + atomic file replace |
| T-3-03 | Denial | 5-branch recovery cost explosion | mitigate | Wave 3: bounded cycles (max 2) + circuit breaker |
| T-3-04 | Race | race condition between branches and verifier | mitigate | Wave 3: reuse Phase 2's anyio cancel-scope pattern |
| T-3-05 | Spoofing | recovery-induced double-action | mitigate | Wave 2: branches reuse Phase 2's IdempotencyTokenStore.try_claim |
| T-3-06 | Information | cassette schema drift | mitigate | Wave 4: cassette includes schema_version field; replay validates |

</threat_model>

<verification>
- All 5 Wave-0 stub test files exist in tests/unit/recovery/
- pytest.importorskip pattern prevents collection errors
- conftest.py fixtures importable and used by stubs
- basicctrl/recovery/__init__.py module importable
</verification>

<success_criteria>
- `pytest tests/unit/recovery/ --collect-only -q` shows 5 test files collected, 5 skipped (import guard)
- `python -c "import basicctrl.recovery; from tests.unit.recovery.conftest import failure_ctx_factory"` succeeds
- Git status shows basicctrl/recovery/__init__.py + tests/unit/recovery/{__init__,conftest,test_*.py} created
</success_criteria>

<output>
After completion, create `.planning/phases/03-recovery-cache-write-back/03-01-SUMMARY.md` recording:
- Module scaffold created at basicctrl/recovery/
- Fixture pattern established (failure_ctx_factory, verifier_mock, etc.)
- 5 Wave-0 stub test files ready for real tests
- Pattern: importorskip at module top + placeholder test prevents collection errors
</output>
