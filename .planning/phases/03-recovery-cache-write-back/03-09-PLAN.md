---
phase: 03-recovery-cache-write-back
plan: 09
type: auto
wave: 5
depends_on: [03-02, 03-03, 03-04, 03-05, 03-06, 03-07, 03-08]
files_modified:
  - tests/integration/test_recovery_e2e.py
  - tests/integration/test_cassette_e2e.py
  - .planning/phases/03-recovery-cache-write-back/PHASE-3-DEMO.md
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Stale selector injected → cassette replay fails → live re-execute → heal written back → cassette atomically updated"
    - "Recovery orchestrator routes each FailureClass to correct branches"
    - "Circuit breaker trips after 3 consecutive failures; stops auto-recovery; emits event"
    - "Heal-rate budget pauses recovery at >5%; escalates to user"
    - "All 6 Phase 3 requirements (HEAL-01..05, CACHE-01..03) validated end-to-end"
  artifacts:
    - path: tests/integration/test_recovery_e2e.py
      provides: "6 integration tests covering full recovery loop, circuit breaker, heal-rate budget"
      min_lines: 300
    - path: tests/integration/test_cassette_e2e.py
      provides: "4 integration tests covering cassette replay, write-back, stream caching"
      min_lines: 250
    - path: .planning/phases/03-recovery-cache-write-back/PHASE-3-DEMO.md
      provides: "operator runbook mirroring PHASE-1/2-DEMO format"
      min_lines: 200

---

# Phase 03 Plan 09: Phase 3 Ship Gate (Integration Tests + Operator Runbook)

**Create 6 recovery + 4 cassette integration tests validating all 8 Phase 3 requirements; write PHASE-3-DEMO.md operator runbook; enable Phase 3 completion and Phase 4 startup.**

## Objective

Validate the full recovery + caching system end-to-end. Ship Phase 3 with operator runbook documenting success criteria, manual pre-flight, per-test demo invocations, automated test suite, 4 manual smoke checks, pitfall mitigations, and failure recovery procedures.

Purpose: Gate Phase 3 completion; validate all HEAL-01..05 + CACHE-01..03 requirements; provide operator instructions for verifying recovery/cache correctness.

Output: `tests/integration/test_recovery_e2e.py` (6 tests) + `test_cassette_e2e.py` (4 tests) + `PHASE-3-DEMO.md` runbook.

## Execution Context

@.planning/phases/03-recovery-cache-write-back/03-CONTEXT.md
@cua_overlay/recovery/ (all Wave 1-3 modules)
@cua_overlay/cache/ (all Wave 4 modules)
@.planning/phases/02-translators-racing/PHASE-2-DEMO.md (template)

## Tasks

<task type="auto">
  <name>Task 1: Write recovery integration tests (6 tests covering all FailureClass routes + circuit breaker + heal budget)</name>
  <files>tests/integration/test_recovery_e2e.py</files>
  <action>Create test_recovery_e2e.py with 6 integration tests:

  **Success criteria tests (SC):**

  SC #1 — Stale selector recovery (HEAL-02, HEAL-03):
  - `test_stale_selector_triggers_b1_rescroll_recovery()` — Inject stale selector (old AX path), trigger action, verify fails → B1 rescroll/retry → succeeds, HealEvent emitted
  - App: Calculator (fast, no auth)
  - Expected: recovery_succeeded event, HealEvent.locator_tier in {AXIdentifier, AXLabel}

  SC #2 — FailureClass routing (HEAL-01):
  - `test_failure_class_perceptual_routes_to_b1_b2_b4()` — Craft failure with low confidence (0.05), verify FailureClassifier returns PERCEPTUAL, RecoveryOrchestrator attempts branches [B1, B2, B4]
  - Use mocked verifier + translator (no real app)
  - Expected: branch attempt events for B1, B2, B4 (not B3, B5)

  SC #3 — Circuit breaker trip (HEAL-05):
  - `test_circuit_breaker_trips_after_3_consecutive_failures()` — Simulate 3 failures on same (bundle_id, target_key), assert is_tripped() returns True, recovery skipped on 4th attempt, circuit_breaker_tripped event emitted
  - Use mocked RaceOrchestrator + verifier (no real app)
  - Expected: circuit_breaker_tripped event, translator_priority reordered

  SC #4 — Heal-rate budget pauses recovery (HEAL-05):
  - `test_heal_rate_budget_exceeded_skips_recovery()` — Set heal_event_count high (>5% of total_actions), trigger recovery, assert recovery_skipped_heal_budget_exceeded event, escalate_to_user called
  - Expected: escalation message includes "heal budget exceeded"

  SC #5 — Bounded recovery cycles (HEAL-04):
  - `test_bounded_recovery_escalates_after_2_cycles()` — All branches fail both cycles, assert recovery_exhausted event, escalate_to_user called
  - Expected: recovery_log has 2 cycles worth of branch_failed events

  SC #6 — Complete recovery flow end-to-end (all HEAL requirements):
  - `test_full_recovery_loop_stale_selector_to_heal_writeback()` — Real Calculator app, inject stale selector, action fails, recovery runs, heals, writeback updates cassette, cassette replay succeeds on re-run
  - Expected: recovery_succeeded → HealEvent emitted → cassette updated

  Use mocked or real Calculator.app per test (SC #1/#6 use real, SC #2-#5 use mocks).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_recovery_e2e.py -m integration -v 2>&1 | grep -c "PASSED"</automated>
  </verify>
  <done>test_recovery_e2e.py has 6 integration tests covering FailureClass routing, circuit breaker, heal budget, bounded cycles, full loop</done>
</task>

<task type="auto">
  <name>Task 2: Write cassette integration tests (4 tests covering replay, write-back, stream caching, schema versioning)</name>
  <files>tests/integration/test_cassette_e2e.py</files>
  <action>Create test_cassette_e2e.py with 4 integration tests:

  SC #7 — Cassette replay success (CACHE-02):
  - `test_cassette_replay_all_steps_match()` — Record 3-step cassette on Calculator, replay, all steps match via pHash, assert (True, None, events)
  - Expected: cassette_step_replay_ok events for all 3 steps

  SC #8 — Cassette replay mismatch triggers live (CACHE-02):
  - `test_cassette_replay_mismatch_fallthroughs_to_live()` — Record 3-step cassette, modify UI state between step 1 and 2, replay, detect mismatch, fallthrough to RaceOrchestrator.execute(), assert (False, 1, events)
  - Expected: cassette_mismatch event at step_idx=1, subsequent live execution

  SC #9 — Write-back with stable-tier gate (CACHE-02, CACHE-03):
  - `test_writeback_stable_tier_updates_cassette()` — HealEvent with locator_tier="AXLabel" applied, cassette updated, Vision-tier heal rejected
  - Record cassette, simulate heal, call writeback, assert cassette file updated atomically
  - Expected: cassette_writeback event, healed_selectors populated

  SC #10 — Stream caching transparent (CACHE-03):
  - `test_stream_cache_transparently_caches_chunks()` — Wrap async generator yielding 5 chunks, iterate once (caches), iterate again (replays), assert 0 new generator calls
  - Expected: All 5 chunks available on second iteration without generator re-execution

  Use real Calculator.app for SC #7/#8/#9, mocks for SC #10.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_cassette_e2e.py -m integration -v 2>&1 | grep -c "PASSED"</automated>
  </verify>
  <done>test_cassette_e2e.py has 4 integration tests covering replay, write-back, stream caching, stable-tier gate</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Recovery subsystem (classifier, 5 branches, circuit breaker, orchestrator, heal events)
    - Caching subsystem (AgentCache, Cassette, replay, write-back, stream caching)
    - 6 recovery integration tests (FailureClass routing, circuit breaker, heal budget, bounded cycles, full loop)
    - 4 cassette integration tests (replay, mismatch, write-back, stream caching)
    - 10 success criteria (SC #1-#10) validating all 8 HEAL/CACHE requirements
  </what-built>
  <how-to-verify>
    1. Run full test suite: `uv run pytest tests/integration/test_recovery_e2e.py tests/integration/test_cassette_e2e.py -m integration -v`
    2. Verify all 10 SC tests PASS (no failures or skips)
    3. Spot-check one recovery test manually: `uv run pytest tests/integration/test_recovery_e2e.py::test_stale_selector_triggers_b1_rescroll_recovery -s` (if running real Calculator, watch the action happen)
    4. Spot-check one cassette test: `uv run pytest tests/integration/test_cassette_e2e.py::test_cassette_replay_all_steps_match -s`
    5. Verify recovery_log.ndjson and heals.ndjson exist under ~/.cua/sessions/<latest>/ with structured events
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 SC tests pass, or describe any failures</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write PHASE-3-DEMO.md operator runbook (mirror PHASE-1-DEMO/PHASE-2-DEMO format)</name>
  <files>.planning/phases/03-recovery-cache-write-back/PHASE-3-DEMO.md</files>
  <action>Create PHASE-3-DEMO.md with structure:

  **Pre-flight** (5 items):
  - Phase 1 + Phase 2 dependencies working (PHASE-1-DEMO, PHASE-2-DEMO completed)
  - Calculator.app installed (for SC #1, #6, #7, #8, #9)
  - ~/.cua/sessions directory writable
  - structlog + asyncio working (inherited from Phase 1)

  **Per-SC demo invocation** (10 SC entries):
  - SC #1: Stale selector recovery
    - Manual: Open Calculator, record "5" button click, corrupt selector in cassette, re-run → observe recovery branch
    - Automated: `uv run pytest tests/integration/test_recovery_e2e.py::test_stale_selector_triggers_b1_rescroll_recovery -s`
    - Expected output: recovery_succeeded event, HealEvent logged
  
  - SC #2-#10: Similar structure (manual + automated per test)

  **Automated test commands**:
  - Full suite: `uv run pytest tests/integration/test_recovery_e2e.py tests/integration/test_cassette_e2e.py -m integration -v`
  - Recovery only: `uv run pytest tests/integration/test_recovery_e2e.py -m integration -v`
  - Cassette only: `uv run pytest tests/integration/test_cassette_e2e.py -m integration -v`
  - Skip integration: `SKIP_INTEGRATION=1 uv run pytest tests/unit/recovery tests/unit/cache -v` (unit only)

  **4 manual smoke checks**:
  1. Circuit breaker reordering visible in logs (grep for "translator_priority reordered")
  2. Heal-rate budget pauses on high heal count (set _heal_event_count high, verify escalate event)
  3. Cassette files created at ~/.cua/sessions/<id>/cassettes/ with correct structure
  4. Atomic write-back doesn't leave .tmp files (no partial cassettes on unclean shutdown)

  **Pitfall mitigations table** (4 Phase 3 pitfalls):
  - P20 (silent regression masking): Heal-rate budget + HealEvent emission defeats 41% abandonment
  - P23 (cassette write-back loop): Stable-tier gate + atomic replace prevents infinite loop
  - P26 (5-branch cost explosion): Bounded cycles (max 2) + circuit breaker prevent runaway
  - P27 (non-determinism baseline): Phase 1 push events + Phase 2 idempotency ensure deterministic recovery paths

  **Failure recovery** (5 common issues + fixes):
  1. Recovery skipped by circuit breaker → manual `clear_circuit_breaker(bundle_id)` MCP tool
  2. Cassette replay mismatch on every step → UI change; record new cassette or manual edit
  3. Heal-rate budget paused → high heal count indicates instability; audit recent changes
  4. Write-back fails to disk → permissions issue on ~/.cua/sessions/; check TCC
  5. Stream cache stale chunks → RL training may benefit; Phase 4 online learning refines

  **Phase exit checklist** (15 items):
  - [ ] All 10 SC tests PASS (no failures, no skips)
  - [ ] Unit test suite passes: `uv run pytest tests/unit/recovery tests/unit/cache -v`
  - [ ] Integration test suite passes: `uv run pytest tests/integration/test_recovery_e2e.py tests/integration/test_cassette_e2e.py -m integration -v`
  - [ ] Manual smoke check #1: circuit breaker reordering visible
  - [ ] Manual smoke check #2: heal-rate budget pauses on high count
  - [ ] Manual smoke check #3: cassette files created with correct structure
  - [ ] Manual smoke check #4: no .tmp files left after write-back
  - [ ] FailureClass enum has 6 variants
  - [ ] RecoveryOrchestrator max_cycles=2 enforced
  - [ ] HealEvent is_stable_tier gate prevents Vision/Coordinate write-back
  - [ ] Cassette.schema_version versioning prevents drift (P-06)
  - [ ] CircuitBreaker emits structured events
  - [ ] All 5 branches (B1-B5) in recovery dispatch table
  - [ ] RaceOrchestrator.execute reachable from recovery branches (no import errors)
  - [ ] PHASE-3-DEMO.md exists with all 5 sections

  Include intro: "Phase 3 enables deterministic self-healing: failures trigger 5-branch parallel recovery, selectors heal, cassettes self-improve via write-back. This demo validates all 6 failure class routes, circuit breaker trip, heal budget pauses, bounded cycles, and cache hit fast paths."
  </action>
  <verify>
    <automated>grep -c "SC #" .planning/phases/03-recovery-cache-write-back/PHASE-3-DEMO.md | head -1</automated>
  </verify>
  <done>PHASE-3-DEMO.md exists with pre-flight, 10 SC invocations, 4 manual smoke checks, pitfall mitigations, failure recovery, phase-exit checklist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

All threats mitigated by prior plans (03-02..03-08). Integration tests verify end-to-end.

## STRIDE Threat Register

All T-3-01..T-3-06 covered by prior plans. Integration tests validate mitigations.

</threat_model>

<verification>
- All 10 SC integration tests pass
- All 8 Phase 3 requirements (HEAL-01..05, CACHE-01..03) validated
- PHASE-3-DEMO.md documents all tests + smoke checks + exit criteria
- Human verifies 4 manual smoke checks; all pass
</verification>

<success_criteria>
- `uv run pytest tests/integration/test_recovery_e2e.py tests/integration/test_cassette_e2e.py -m integration -v` shows 10 PASSED, 0 FAILED, 0 SKIPPED
- `grep -c "SC #" .planning/phases/03-recovery-cache-write-back/PHASE-3-DEMO.md` returns 10
- `grep -c "Phase exit checklist" .planning/phases/03-recovery-cache-write-back/PHASE-3-DEMO.md` returns 1
- All 4 manual smoke checks documented and user confirms passed
- Git log shows atomic commits for each test file + demo runbook
</success_criteria>

<output>
After completion, create `.planning/phases/03-recovery-cache-write-back/03-09-SUMMARY.md` recording:
- 10 success criteria integration tests (6 recovery + 4 cassette)
- All 8 Phase 3 requirements (HEAL-01..05, CACHE-01..03) validated e2e
- PHASE-3-DEMO.md operator runbook with pre-flight, manual smoke checks, failure recovery, phase-exit checklist
- Phase 3 ship gate complete; ready for Phase 4 startup
- Pattern: SC tests mirror Phase 1/2 structure; runbook follows established operator workflow
</output>
