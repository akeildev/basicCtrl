---
phase: 06-private-spis-durability-hardening
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - basicctrl/spi/webkit_inspector.py
  - tests/test_spi_webkit.py
autonomous: true
requirements: [SPI-07]
user_setup: []

must_haves:
  truths:
    - "WebKit RemoteInspector private headers accessible for Safari deep access"
    - "Safari automation can introspect JS context (optional feature)"
    - "Graceful fallback to T3 AppleScript do JavaScript if unavailable"

---

<objective>
Implement SPI-07 WebKit RemoteInspector wrapper for Safari deep access. Enables Safari JavaScript introspection as alternative to AppleScript.

Purpose: Unlock Safari renderer introspection. Optional high-power channel for Safari automation (already has T3 AppleScript fallback).

Output: 1 Python wrapper + unit tests. Ready for integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md#phase-6
@.planning/REQUIREMENTS.md#spi-07
@.planning/phases/06-private-spis-durability-hardening/06-RESEARCH.md#webkit
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WebKit RemoteInspector wrapper</name>
  <files>basicctrl/spi/webkit_inspector.py</files>
  <read_first>
    - RESEARCH.md §"WebKit RemoteInspector" L35, L50
  </read_first>
  <action>
Create basicctrl/spi/webkit_inspector.py:

```python
"""SPI-07: WebKit RemoteInspector for Safari deep access."""
import structlog

log = structlog.get_logger(__name__)

class WebKitInspectorBridge:
    """Wrapper for WebKit RemoteInspector private headers (Safari)."""
    
    def __init__(self, available: bool = False):
        self.available = available
        if available:
            log.info("webkit_inspector_bridge_loaded", available=True)
    
    async def evaluate_js_in_safari(self, script: str) -> str:
        """Evaluate JavaScript in Safari (optional)."""
        if not self.available:
            log.warning("webkit_inspector_unavailable; fallback to AppleScript")
            return None
        # Implementation deferred: RemoteInspector protocol is undocumented
        return None

_bridge = None

async def get_webkit_inspector_bridge(capabilities):
    global _bridge
    if _bridge is None:
        _bridge = WebKitInspectorBridge(available=capabilities.webkit_inspector_available)
    return _bridge
```
  </action>
  <verify>
    <automated>
      pytest tests/test_spi_webkit.py -x
    </automated>
  </verify>
  <done>
    WebKitInspectorBridge created as stub for future Safari deep access.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests for WebKit wrapper</name>
  <files>tests/test_spi_webkit.py</files>
  <action>
Create tests/test_spi_webkit.py as stub tests for WebKit RemoteInspector.

```python
"""Tests for SPI-07 WebKit RemoteInspector (stub)."""
import pytest
from basicctrl.spi.webkit_inspector import WebKitInspectorBridge, get_webkit_inspector_bridge

def test_webkit_inspector_bridge_init():
    bridge = WebKitInspectorBridge(available=False)
    assert bridge.available is False

@pytest.mark.asyncio
async def test_get_webkit_inspector_bridge_caches():
    # Mock capabilities
    from basicctrl.spi.probe import SPICapabilities
    caps = SPICapabilities(
        skylight_available=False, ax_remote_available=False,
        cgs_display_space_available=False, endpoint_security_available=False,
        dtrace_available=False, dyld_inject_available=False,
        webkit_inspector_available=False, imu_available=False,
    )
    bridge1 = await get_webkit_inspector_bridge(caps)
    bridge2 = await get_webkit_inspector_bridge(caps)
    assert bridge1 is bridge2
```
  </action>
  <verify>
    <automated>
      pytest tests/test_spi_webkit.py -x
    </automated>
  </verify>
  <done>
    WebKit tests created. Stub implementation ready for future enhancement.
  </done>
</task>

</tasks>

<success_criteria>
- [ ] basicctrl/spi/webkit_inspector.py created (stub)
- [ ] tests/test_spi_webkit.py created
- [ ] All tests passing

</success_criteria>

<output>
After completion, create `.planning/phases/06-private-spis-durability-hardening/06-04-SUMMARY.md`
</output>
