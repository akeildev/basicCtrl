---
phase: 01-foundation-state-verifier
plan: 09
type: execute
wave: 5
depends_on: ["01-02", "01-03", "01-04", "01-05", "01-06", "01-07", "01-08"]
files_modified:
  - cua_overlay/demo/__init__.py
  - cua_overlay/demo/calculator_click.py
  - tests/integration/test_calculator_click.py
  - tests/integration/test_phase1_e2e.py
  - .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md
autonomous: true
requirements:
  - VERIFY-04
  - VERIFY-05
must_haves:
  truths:
    - "Calculator click on the '5' button fires kAXValueChanged AND is recorded as VERIFIED in <50ms via L0+L1 — Phase 1 ROADMAP success criterion 1"
    - "L2 verifier is NOT invoked during the Calculator demo (success criterion 4: '<50ms with no AX subtree walk')"
    - "L3 stub is NEVER invoked during the Calculator demo (success criterion: Phase 1 invariant)"
    - "State graph round-trips: probe Calculator '5' button → upsert UIElement → read back via composite_key — equality holds (success criterion 2)"
    - "AppProfile cache file ~/.cua/profiles/com.apple.Calculator.json exists after the demo and contains ax_rich=True (success criterion 3)"
    - "AX rate-limit (TokenBucket 20/sec/pid) is exercised — verified by structured event count in action_log.ndjson (success criterion 5)"
    - "MCP proxy lists upstream tools + click_with_healing; click_with_healing call against Calculator '5' button returns success dict + action_log entry (success criterion 6)"
    - "PHASE-1-DEMO.md documents the run: command, expected output, measured latencies, manual smoke checks (TCC, modal)"
  artifacts:
    - path: "cua_overlay/demo/calculator_click.py"
      provides: "Standalone runnable: `python -m cua_overlay.demo.calculator_click` that launches Calculator, runs the full Phase 1 stack, prints VERIFIED + latency, exits 0 on success"
      contains: "from cua_overlay.profile import classify"
    - path: "tests/integration/test_calculator_click.py"
      provides: "End-to-end pytest equivalents of the demo: test_under_50ms, test_l2_l3_not_invoked, test_state_graph_roundtrip, test_axvalue_changed"
      contains: "test_under_50ms\\|test_l2_l3_not_invoked"
    - path: "tests/integration/test_phase1_e2e.py"
      provides: "All-six-success-criteria smoke test running in one pytest"
      contains: "ROADMAP success criteria"
    - path: ".planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md"
      provides: "Operator runbook: pre-flight (TCC, Postgres), commands, expected output, manual checks (P24 TCC, P25 modal)"
      contains: "scripts/init_postgres.sh"
  key_links:
    - from: "cua_overlay/demo/calculator_click.py"
      to: "cua_overlay.profile.classify + AXEventBridge + AXObserverManager + Aggregator + SessionWriter + DurableExecutor"
      via: "Single coroutine wires the full stack"
      pattern: "classify|AXEventBridge|Aggregator"
    - from: "tests/integration/test_calculator_click.py"
      to: "All Phase 1 ROADMAP success criteria"
      via: "Each criterion → one pytest test"
      pattern: "test_under_50ms|test_state_graph_roundtrip|test_appprofile_cache_persists|test_l2_l3_not_invoked"
threat_model:
  trust_boundaries: []
  threats: []
---

<objective>
Phase 1 final integration: demonstrate the canonical "Calculator click verified in <50ms" end-to-end demo. This is the ROADMAP success criterion that proves Phase 1 ships.

This plan is the integration test — it exercises EVERY prior plan in this phase: scaffold (01), AppProfile classifier (02), AX safety primitives (03), AXObserver bridge (04), L0+L1 ensemble (05), L2+L3 ladder (06), persistence (07), MCP proxy (08).

Per ROADMAP Phase 1 Success Criteria (verbatim from ROADMAP.md):
1. Click in Calculator fires `kAXValueChanged` and is recorded as VERIFIED in <50ms via L0 push subscription (subscribed BEFORE action fires).
2. State graph round-trips: probe Calculator → write `UIElement` entity → read it back with stable composite key (role_path + label + bbox_centroid), not raw AXUIElement ref.
3. AppProfile classifier caches per-bundle capability probe (AX-rich? .sdef? CDP-port?) and survives session restart.
4. L0 push + L1 cheap diff (CGWindowList, NSPasteboard.changeCount, dHash) verifies a click in <50ms with no AX subtree walk.
5. AX rate-limiter caps at 20 calls/sec/pid; depth-limited subtree (3 levels max) prevents Safari hangs.
6. Existing trycua MCP server surface still works; healing wrapper exposed as additional MCP tools so Claude Code / Cursor / Codex can invoke it.

Output:
- Standalone demo script: `cua_overlay/demo/calculator_click.py` runnable as `python -m cua_overlay.demo.calculator_click`
- pytest integration test mirroring the demo: `tests/integration/test_calculator_click.py`
- Phase-1 end-to-end smoke test: `tests/integration/test_phase1_e2e.py` covering all 6 success criteria
- Operator runbook: `PHASE-1-DEMO.md` documenting pre-flight, commands, expected output, manual checks
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-state-verifier/01-RESEARCH.md
@.planning/phases/01-foundation-state-verifier/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Demo script — calculator_click.py runnable end-to-end</name>
  <read_first>
    - .planning/phases/01-foundation-state-verifier/01-RESEARCH.md (section "Calculator click verified in <50ms (the Phase 1 demo)" — full code example)
    - .planning/ROADMAP.md (Phase 1 success criteria, full text)
    - cua_overlay/profile/__init__.py (Plan 02 exports)
    - cua_overlay/ax/__init__.py (Plan 03 exports)
    - cua_overlay/verifier/__init__.py (Plan 04-06 exports)
    - cua_overlay/persist/__init__.py (Plan 07 exports)
    - cua_overlay/state/__init__.py (Plan 01 exports)
    - cua_overlay/log.py
  </read_first>
  <files>cua_overlay/demo/__init__.py, cua_overlay/demo/calculator_click.py</files>
  <behavior>
    - Test 1: `uv run python -m cua_overlay.demo.calculator_click` exits 0 when Calculator + TCC + Postgres are available.
    - Test 2: Demo prints a structured summary including: session_id, ax_rich=True, ax_observer_works=True, latency_ms (<50), confidence (>=0.5), L2_signal=None, L3_signal=None.
    - Test 3: After running, `~/.cua/profiles/com.apple.Calculator.json` exists with valid JSON.
    - Test 4: After running, `~/.cua/sessions/<session_id>/action_log.ndjson` exists with at least one valid JSON line.
  </behavior>
  <action>
1. **Create `cua_overlay/demo/__init__.py`** (empty).

2. **Create `cua_overlay/demo/calculator_click.py`** as a standalone runnable. The script must:

   **Imports (top of file):**
   - asyncio, subprocess, sys, time, signal, os, datetime
   - from rich.console import Console (for pretty output)
   - structlog
   - from cua_overlay.log import configure as configure_logging
   - from cua_overlay.profile import classify
   - from cua_overlay.ax import TokenBucket, walk_subtree, AXUIElementWrapper, has_blocking_modal
   - from cua_overlay.ax.observer import AXEventBridge
   - from cua_overlay.verifier import AXObserverManager, NSWorkspaceObserver, KqueueProcObserver, L0Push, L1Cheap, L2Medium, L3Stub, WeightedVote, Aggregator, VERIFIED_THRESHOLD
   - from cua_overlay.state import UIElement, StateGraph
   - from cua_overlay.state.causal_dag import ActionCanonical, HoarePre, CausalDAG
   - from cua_overlay.state.ring_buffer import TemporalRingBuffer, StateSnapshot
   - from cua_overlay.persist import SessionWriter, DurableExecutor

   **Helper `_launch_calculator() -> int`:**
   - `subprocess.run(["open", "-a", "Calculator"], check=True)`
   - poll NSWorkspace.runningApplications for `bundleIdentifier == "com.apple.Calculator"` with 5s timeout
   - return processIdentifier as int

   **Helper `_find_button5(pid: int, bundle_id: str) -> tuple[UIElement, Any]`:**
   - Build the AXUIElementApplication ref via `AXUIElementCreateApplication(pid)`
   - Run `walk_subtree(app, pid=pid, bundle_id=bundle_id, max_depth=3)` to enumerate buttons
   - Find a UIElement with `role == "AXButton"` and `label == "5"` (Calculator localizes; fallback to label.strip() in `("5", "Five")`)
   - Return (UIElement, raw AX child ref) — keep BOTH so the AXObserverManager.expect call has the AX ref, while the state graph stores the typed UIElement

   **Helper `_fire_cgevent_click(x: int, y: int) -> None`:**
   - Use Quartz CGEventCreateMouseEvent + CGEventPost with kCGEventLeftMouseDown then kCGEventLeftMouseUp at (x, y), button = kCGMouseButtonLeft, tap = kCGHIDEventTap

   **REQUIRED public surface — TWO callables:**

   - `async def run_demo() -> dict`: the testable core. Sets up the full Phase 1 stack, fires the click, runs the verifier, and **returns** a result dict (shape spelled out in Task 2 step 1 — keep them in sync). Does NOT print, does NOT call sys.exit. Lets pytest assert against the return value directly without parsing rich output.
   - `async def main() -> int`: thin CLI wrapper. Calls `run_demo()`, pretty-prints with `rich.console.Console`, returns exit code (0 on success, 1 on any AssertionError caught from run_demo).

   `run_demo() -> dict` MUST return:
   ```python
   {
       "session_id": str,
       "composite_key": str,
       "confidence": float,
       "elapsed_ms": float,
       "tier_signals": dict[str, Optional[float]],
       "profile": dict,                 # AppProfile.model_dump(mode="json")
       "post": dict,                    # HoarePost.model_dump(mode="json")
       "action_log_path": str,
       "appprofile_cache_path": str,
       "verified": bool,                # post.verified
   }
   ```

   **Main coroutine `async def main() -> int`:**
   1. `configure_logging()`
   2. `console = Console()`
   3. `console.rule("[bold cyan]Phase 1 Calculator demo[/bold cyan]")`
   4. Launch Calculator: `pid = await asyncio.to_thread(_launch_calculator)`; sleep 0.5s for app to settle.
   5. **Success criterion 3:** `profile = await classify("com.apple.Calculator", pid)`; `console.print(f"AppProfile: ax_rich={profile.ax_rich}, ax_observer_works={profile.ax_observer_works}, latency_ms={profile.probe_latency_ms}")`. Assert `ax_rich is True`.
   6. **Set up the full Phase 1 stack:**
      - `loop = asyncio.get_running_loop()`
      - `bridge = AXEventBridge(loop=loop); bridge.start()`
      - `axmgr = AXObserverManager(bridge=bridge); axmgr.start()`
      - `ws = NSWorkspaceObserver(loop=loop); ws.start()`
      - inside `async with KqueueProcObserver(loop=loop) as kq:` build verifier ensemble + persistence (matching Plan 08's main.py boot)
      - `session = SessionWriter()`; `durable = DurableExecutor()`; try `await durable.setup()` — log warning on failure but continue
   7. **Resolve "5" button:** `(button5_elem, button5_axref) = await asyncio.to_thread(_find_button5, pid, "com.apple.Calculator")`
   8. **Success criterion 2 — state graph round-trip:**
      ```python
      graph = StateGraph()
      graph.upsert(button5_elem)
      assert graph.get(button5_elem.composite_key) is button5_elem, "round-trip failed"
      console.print(f"State graph round-trip OK: composite_key={button5_elem.composite_key}")
      ```
   9. **Build action + Hoare pre-condition:**
      - `action_id = "demo-click-001"; step_idx = 1`
      - `pre = HoarePre(target_key=button5_elem.composite_key, target_exists=True, target_enabled=True, target_role="AXButton", role_compatible=True, frontmost_app="com.apple.Calculator", no_blocking_modal=(await has_blocking_modal(pid, bundle_id="com.apple.Calculator") is None), timestamp_ns=time.monotonic_ns())`
      - `assert pre.no_blocking_modal, "modal blocking the demo — close any system dialogs"`
   10. **L1 baseline:** `l1_before = await L1Cheap().snapshot(button5_elem)`
   11. **PRE-SUBSCRIBE (the secret weapon):** call `axmgr.expect(target=button5_elem, notifs=["AXValueChanged", "AXFocusedUIElementChanged"], action_id=action_id, timeout_ms=500, ax_element=button5_axref)` — this is the subscribe-before-fire pattern. The returned future is the L0 signal.
   12. **FIRE:** `t_start = time.monotonic()`; call `_fire_cgevent_click(int(button5_elem.bbox.centroid[0]), int(button5_elem.bbox.centroid[1]))`
   13. **Build action + run aggregator:**
       ```python
       action = ActionCanonical(
           id=action_id, step_idx=step_idx, kind="MUTATE",
           target_key=button5_elem.composite_key, action_type="click",
           payload={"x": button5_elem.bbox.centroid[0], "y": button5_elem.bbox.centroid[1]},
           tier=None, channel="C3",  # CGEvent
           timestamp_ns=time.monotonic_ns(),
           session_id=session.session_id,
       )
       post = await aggregator.verify(
           action=action, target=button5_elem,
           notifs=["AXValueChanged", "AXFocusedUIElementChanged"],
           before_l1=l1_before, ax_element=button5_axref, timeout_ms=50,
       )
       elapsed_ms = (time.monotonic() - t_start) * 1000.0
       ```
   14. **Success criteria 1 + 4:**
       ```python
       assert post.verified, f"verifier failed: confidence={post.confidence}"
       assert elapsed_ms < 50, f"latency {elapsed_ms}ms > 50ms budget"
       assert post.tier_signals.get("L2") is None, "L2 was invoked — Phase 1 invariant violated (success criterion 4)"
       assert post.tier_signals.get("L3") is None, "L3 was invoked — Phase 1 invariant violated"
       ```
   15. **Append action log + Postgres checkpoint:**
       ```python
       session.append_action_log({
           "step_idx": step_idx, "action_id": action_id, "tool": "click",
           "pre": pre.model_dump(mode="json"), "action": action.model_dump(mode="json"),
           "post": post.model_dump(mode="json"), "elapsed_ms": elapsed_ms,
       })
       try:
           await durable.checkpoint(session_id=session.session_id, step_idx=step_idx, pre=pre, action=action, post=post)
           console.print("[green]Postgres checkpoint written[/green]")
       except Exception as e:
           console.print(f"[yellow]Postgres checkpoint failed (continuing): {e}[/yellow]")
       ```
   16. **Print structured success summary:**
       ```python
       console.rule("[bold green]VERIFIED[/bold green]")
       console.print(f"session_id    = {session.session_id}")
       console.print(f"composite_key = {button5_elem.composite_key}")
       console.print(f"confidence    = {post.confidence:.3f}")
       console.print(f"latency_ms    = {elapsed_ms:.2f}")
       console.print(f"L0 signal     = {post.tier_signals.get('L0')}")
       console.print(f"L1 signal     = {post.tier_signals.get('L1')}")
       console.print(f"L2 signal     = {post.tier_signals.get('L2')}  (None expected for Phase 1)")
       console.print(f"L3 signal     = {post.tier_signals.get('L3')}  (None expected for Phase 1)")
       ```
   17. **Cleanup:** `await axmgr.stop(); bridge.stop(); ws.stop(); await durable.aclose()`
   18. The body above (steps 5-17) is `run_demo()`. It returns the result dict. `main()` then unpacks the dict, pretty-prints via `rich`, and returns 0 on success (or 1 if `run_demo()` raised AssertionError — `main()` catches and logs, never re-raises).

   **At the bottom:**
   ```python
   if __name__ == "__main__":
       sys.exit(asyncio.run(main()))
   ```

3. **Verify (manual):** `uv run python -m cua_overlay.demo.calculator_click` exits 0 with green VERIFIED panel. Latency printed.
  </action>
  <verify>
    <automated>uv run python -c "import cua_overlay.demo.calculator_click; print('importable')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `cua_overlay/demo/calculator_click.py` and is at least 100 lines (full demo non-trivial).
    - `grep -c -E "(async def run_demo|def run_demo)" cua_overlay/demo/calculator_click.py` returns at least 1 (run_demo callable factored out for Task 2 tests to import directly).
    - `grep -c "from cua_overlay.profile import classify\\|cua_overlay.profile" cua_overlay/demo/calculator_click.py` returns at least 1.
    - `grep -c "AXEventBridge\\|AXObserverManager" cua_overlay/demo/calculator_click.py` returns at least 2.
    - `grep -c "Aggregator" cua_overlay/demo/calculator_click.py` returns at least 1.
    - `grep -c "SessionWriter\\|DurableExecutor" cua_overlay/demo/calculator_click.py` returns at least 2.
    - `grep -c "kAXValueChanged\\|AXValueChanged" cua_overlay/demo/calculator_click.py` returns at least 1.
    - `grep -c "elapsed_ms < 50\\|< 50" cua_overlay/demo/calculator_click.py` returns at least 1 (success criterion 1 latency assertion).
    - `grep -c "tier_signals.*L2\\|L2.*is None\\|L2.*None" cua_overlay/demo/calculator_click.py` returns at least 1 (success criterion 4 invariant assertion).
    - `grep -c "expect.*notifs\\|axmgr.expect" cua_overlay/demo/calculator_click.py` returns at least 1 (subscribe-before-fire pattern present).
    - Module importable: `uv run python -c "import cua_overlay.demo.calculator_click"` exits 0.
    - With Calculator + TCC + Postgres: `uv run python -m cua_overlay.demo.calculator_click` exits 0 (manual verification).
  </acceptance_criteria>
  <done>Standalone demo script wires every Phase 1 component end-to-end. Calculator click verified in <50ms with L0+L1 only. State graph round-trip OK. AppProfile cached. Action log + Postgres checkpoint written. Rich console output prints structured summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pytest integration tests covering all 6 ROADMAP success criteria</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria — full text)
    - .planning/phases/01-foundation-state-verifier/01-VALIDATION.md (Per-Task Verification Map rows for VERIFY-04, VERIFY-05, success criteria)
    - cua_overlay/demo/calculator_click.py (Task 1)
    - tests/conftest.py (calculator_pid fixture)
    - tests/integration/test_app_profile.py (Plan 02 — for cache-persist patterns)
    - tests/integration/test_axobserver.py (Plan 04 — for AXObserver patterns)
  </read_first>
  <files>tests/integration/test_calculator_click.py, tests/integration/test_phase1_e2e.py</files>
  <behavior>
    - Test 1 (test_calculator_click.py::test_under_50ms): Re-runs the demo logic minimally; asserts elapsed_ms < 50 across 3 consecutive runs (allows for one warmup run, picks the median of last 2).
    - Test 2 (test_calculator_click.py::test_l2_l3_not_invoked): After click+verify, post.tier_signals["L2"] is None AND post.tier_signals["L3"] is None.
    - Test 3 (test_calculator_click.py::test_axvalue_changed): The L0 signal max for prefix "ax." is >= 1.0 (kAXValueChanged or similar fired).
    - Test 4 (test_calculator_click.py::test_state_graph_roundtrip): probe button5 → graph.upsert → graph.get(composite_key) returns same UIElement.
    - Test 5 (test_calculator_click.py::test_appprofile_cache_persists): After demo, ~/.cua/profiles/com.apple.Calculator.json exists; can be parsed back to AppProfile.
    - Test 6 (test_phase1_e2e.py::test_all_six_success_criteria): A single `@pytest.mark.integration` test that walks through all 6 ROADMAP criteria with explicit assertions, printing PASS/FAIL per criterion to stdout. Designed for the Phase 1 ship-gate.
  </behavior>
  <action>
1. **Create `tests/integration/test_calculator_click.py`** with Tests 1-5 above, all marked `@pytest.mark.integration`. Reuse the `run_demo()` callable already exported by `cua_overlay/demo/calculator_click.py` (factored in Task 1) — import it directly: `from cua_overlay.demo.calculator_click import run_demo`. The pytest tests call `run_demo()` and assert on the returned dict (do not parse rich console output).

   The `run_demo() -> dict` shape (must match Task 1's contract verbatim):
   ```python
   {
       "session_id": str,
       "composite_key": str,
       "confidence": float,
       "elapsed_ms": float,
       "tier_signals": dict[str, Optional[float]],
       "profile": dict,                 # AppProfile.model_dump(mode="json")
       "post": dict,                    # HoarePost.model_dump(mode="json")
       "action_log_path": str,
       "appprofile_cache_path": str,
       "verified": bool,                # post.verified
   }
   ```
   No refactor required here — Task 1 already produced this surface. If Task 1 did not, file a bug back against Task 1; do not patch it from Task 2.

2. For Test 1 (under-50ms with median of 3), do something like:
   ```python
   results = []
   for _ in range(3):
       r = await run_demo()
       results.append(r["elapsed_ms"])
       await asyncio.sleep(0.5)  # let Calculator settle between iterations
   median = sorted(results)[1]
   assert median < 50, f"median latency {median}ms > 50ms; samples: {results}"
   ```

3. **Create `tests/integration/test_phase1_e2e.py`** with one `test_all_six_success_criteria` test that:
   - Runs `run_demo()` once
   - Then walks each of the 6 ROADMAP Phase 1 success criteria with explicit asserts and prints (using `pytest`'s `capsys` or just `print()`):
     - SC-1: `r["elapsed_ms"] < 50 AND r["confidence"] >= 0.5` ("Click in Calculator ... <50ms via L0 push subscription")
     - SC-2: state graph round-trip (re-do the upsert+get assertion)
     - SC-3: appprofile cache file exists + valid (`AppProfile.model_validate_json(file.read_text())`)
     - SC-4: `r["tier_signals"]["L2"] is None AND r["tier_signals"]["L3"] is None` ("L0 push + L1 cheap diff verifies a click in <50ms with no AX subtree walk")
     - SC-5: AX rate-limit exercised — count occurrences of structured event `ax.rate_limited` in action_log.ndjson is 0 (we shouldn't have hit the limit during a single click, but the limiter EXISTS — assert TokenBucket().rate == 20.0 instead). Also assert depth-limited subtree default: `from cua_overlay.ax.walker import walk_subtree; sig = inspect.signature(walk_subtree); assert sig.parameters["max_depth"].default == 3`.
     - SC-6: MCP proxy lists upstream + healing tools — spawn the proxy via stdio_client, await session.list_tools(), assert `click_with_healing` in tool names AND at least one upstream tool name (e.g., `screenshot`).

4. **Verify (with full environment: Calculator launchable, TCC granted, cua-driver built, Postgres up):**
   - `uv run pytest -x -v -m integration tests/integration/test_calculator_click.py tests/integration/test_phase1_e2e.py` exits 0.
   - All 6 SC checks pass in test_phase1_e2e.
  </action>
  <verify>
    <automated>uv run pytest -x -q -m integration tests/integration/test_calculator_click.py tests/integration/test_phase1_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `tests/integration/test_calculator_click.py` and `grep -c "test_under_50ms\\|test_l2_l3_not_invoked\\|test_state_graph_roundtrip\\|test_appprofile_cache_persists\\|test_axvalue_changed" tests/integration/test_calculator_click.py` returns at least 5.
    - `grep -c "@pytest.mark.integration" tests/integration/test_calculator_click.py` returns at least 5.
    - `grep -c "elapsed_ms.*<.*50\\|< 50" tests/integration/test_calculator_click.py` returns at least 1.
    - `grep -c "tier_signals.*L2\\|tier_signals\\[.L2.\\]" tests/integration/test_calculator_click.py` returns at least 1.
    - File exists: `tests/integration/test_phase1_e2e.py` and `grep -c "test_all_six_success_criteria\\|ROADMAP" tests/integration/test_phase1_e2e.py` returns at least 1.
    - `grep -c "SC-1\\|SC-2\\|SC-3\\|SC-4\\|SC-5\\|SC-6" tests/integration/test_phase1_e2e.py` returns at least 6.
    - With full environment: `uv run pytest -x -q -m integration tests/integration/test_calculator_click.py tests/integration/test_phase1_e2e.py` exits 0 with at least 6 PASSED.
    - `cua_overlay/demo/calculator_click.py` exposes a `run_demo()` callable returning the result dict (verified by `grep -c "def run_demo\\|async def run_demo" cua_overlay/demo/calculator_click.py` returns at least 1).
  </acceptance_criteria>
  <done>5 distinct pytest tests in test_calculator_click.py mirror the demo for CI verification. test_phase1_e2e.py walks all 6 ROADMAP success criteria with explicit asserts and prints PASS/FAIL per criterion. Demo is refactored into a reusable `run_demo()` that the tests call directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PHASE-1-DEMO.md operator runbook + manual smoke checks</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria + BLOCKER pitfalls list)
    - .planning/phases/01-foundation-state-verifier/01-VALIDATION.md (Manual-Only Verifications section)
    - cua_overlay/demo/calculator_click.py (Task 1)
    - scripts/init_postgres.sh (from Plan 07)
    - scripts/doctor.py (from Plan 01)
  </read_first>
  <files>.planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md</files>
  <behavior>
    - The runbook is followable by Akeil end-to-end: doctor → init Postgres → build cua-driver → run demo → check structured output → run manual checks.
  </behavior>
  <action>
1. **Create `.planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md`** with these sections:

   **Section: Pre-flight (one-time setup)**
   - Install dependencies: `make install`
   - Provision Postgres: `bash scripts/init_postgres.sh`
   - Build cua-driver: `cd libs/cua-driver && swift build -c release && cd -`
   - Add cua-driver to PATH: `export PATH="$PWD/libs/cua-driver/.build/release:$PATH"` OR `export CUA_DRIVER_BIN=$PWD/libs/cua-driver/.build/release/cua-driver`
   - Verify environment: `make doctor` — all rows must be [OK] (Postgres + Accessibility + Calculator).
   - Grant TCC: System Settings → Privacy & Security → Accessibility → enable for the terminal you're running from.

   **Section: Run the demo**
   - `uv run python -m cua_overlay.demo.calculator_click`
   - Expected output (rich-formatted):
     ```
     ── Phase 1 Calculator demo ──
     AppProfile: ax_rich=True, ax_observer_works=True, latency_ms=312
     State graph round-trip OK: composite_key=path:com.apple.Calculator:AXApplication/AXWindow[0]/AXButton[N]:5
     Postgres checkpoint written
     ── VERIFIED ──
     session_id    = <uuid4>
     composite_key = path:com.apple.Calculator:...
     confidence    = 0.600
     latency_ms    = 18.42
     L0 signal     = 1.0
     L1 signal     = 0.0
     L2 signal     = None  (None expected for Phase 1)
     L3 signal     = None  (None expected for Phase 1)
     ```
   - Exit code: 0.

   **Section: Run automated tests**
   - `uv run pytest -x -q tests/unit/` — all unit tests pass (~50+).
   - `uv run pytest -x -q -m integration tests/integration/` — Calculator integration tests pass (~15+).
   - `uv run pytest -x -v -m integration tests/integration/test_phase1_e2e.py` — all 6 ROADMAP success criteria PASS.

   **Section: Manual smoke checks (1x per phase ship)**
   Per 01-VALIDATION.md "Manual-Only Verifications":
   - **TCC revocation (P24):** Run `python -m cua_overlay.demo.calculator_click` once green. Then disable Accessibility for terminal in System Settings. Re-run. Expect: structured event `tcc_revoked` in stderr/log; process exits 2 with action URL printed.
   - **Modal alert (P25):** Open System Settings → Privacy & Security → click any "Lock" icon to trigger a password prompt. While modal is up, run `python -m cua_overlay.demo.calculator_click`. Expect: assertion failure on `pre.no_blocking_modal` with structured message.
   - **SIGKILL crash-resume (PERSIST-03):** Start `python -m cua_overlay.demo.calculator_click`. While Postgres checkpoint is being written, in another terminal `kill -9 <pid>`. Then run a small script that does `await resume_from_checkpoint(session_id, durable)` for the same session_id. Expect: ResumeContext with last_step_idx=1.

   **Section: Pitfalls verified mitigated**
   - P2 (cmux #2985 / AX rate-limit): TokenBucket caps at 20/sec/pid. Enforced at `cua_overlay/ax/rate_limit.py`. Tests: `test_rate_limit.py::test_initial_burst_grants_20`, `test_21st_call_in_first_second_returns_false`.
   - P3 (full recursive AX = 15-20s on Safari): walker hard-caps depth=3, children=50, nodes=500. Tests: `test_walker.py::test_caps_at_max_depth`, etc.
   - P14 (AX notifs fail on web/Electron): AppProfile has `ax_observer_works` field probed at session start. Phase 2 routes around it.
   - P24 (TCC revoked mid-session): TCCMonitor.check() at every classify() entry. Manual smoke check above.
   - P25 (modal alert blocks AX): has_blocking_modal() probe in HoarePre. Manual smoke check above.
   - P28 (stale notification races verifier): subscription_ts_ns + 5ms guard + action_id filter. Tests: `test_axobserver_filter.py::test_filter_drops_pre_subscription_events`, etc.

   **Section: Failure recovery**
   - "make doctor failed: Postgres not listening" → `brew services start postgresql@16` + re-run `bash scripts/init_postgres.sh`.
   - "FileNotFoundError: cua-driver" → rebuild Swift: `cd libs/cua-driver && swift build -c release` + set `CUA_DRIVER_BIN`.
   - "AssertionError: latency XXms > 50ms budget" → first run can be slow due to module imports / OCR JIT; re-run twice. Median of 3 should pass.
   - "AssertionError: ax_rich is False" → the test was run before Calculator finished launching; the `await asyncio.sleep(0.5)` may need bumping to 1.0s on slow boots.

   **Section: Phase exit checklist**
   - [ ] `make test` exits 0 (all unit tests).
   - [ ] `make doctor` all green.
   - [ ] `python -m cua_overlay.demo.calculator_click` exits 0 with VERIFIED + latency_ms < 50.
   - [ ] `pytest -m integration tests/integration/test_phase1_e2e.py` shows all 6 SC PASS.
   - [ ] Manual TCC revocation smoke check completed.
   - [ ] Manual modal-blocking smoke check completed.
   - [ ] Manual SIGKILL crash-resume smoke check completed.
   - [ ] Per-plan SUMMARY.md files exist for plans 01-09.
   - [ ] No edits under `libs/cua-driver/Sources/` (verified by `git diff --name-only libs/cua-driver/Sources/` returning empty).

2. **Verify:** the file is well-formed markdown, all section headers present.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md && grep -c "Pre-flight\\|Run the demo\\|Manual smoke\\|Pitfalls verified\\|Phase exit checklist" .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md`.
    - `grep -c "## Pre-flight\\|## Run the demo\\|## Manual smoke checks\\|## Pitfalls verified\\|## Phase exit checklist" .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md` returns at least 5.
    - `grep -c "scripts/init_postgres.sh" .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md` returns at least 1.
    - `grep -c "make doctor" .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md` returns at least 1.
    - `grep -c "P2\\|P3\\|P14\\|P24\\|P25\\|P28" .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md` returns at least 6 (all six BLOCKERs documented).
    - `grep -c "tcc_revoked\\|TCC revocation" .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md` returns at least 1.
    - `grep -c "SIGKILL\\|kill -9" .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md` returns at least 1.
    - `grep -c "VERIFIED\\|latency_ms" .planning/phases/01-foundation-state-verifier/PHASE-1-DEMO.md` returns at least 2.
  </acceptance_criteria>
  <done>PHASE-1-DEMO.md is the operator runbook covering pre-flight, demo run, automated tests, manual smoke checks (TCC, modal, SIGKILL), pitfall mitigation citations, failure recovery, and phase exit checklist. Akeil can follow it end-to-end without reading other plans.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

No new threats introduced by this plan. All threats from prior plans (T-1-01..T-1-06) are surfaced in PHASE-1-DEMO.md's Pitfalls section.
</threat_model>

<verification>
Phase 1 ship gate — all of the following must pass:

1. **Unit tests:** `uv run pytest -x -q tests/unit/` exits 0 (~50+ PASSED cumulative across plans 01-08).
2. **Integration tests (with environment):** `uv run pytest -x -v -m integration tests/integration/test_phase1_e2e.py` exits 0 with all 6 ROADMAP success criteria PASS.
3. **Demo:** `uv run python -m cua_overlay.demo.calculator_click` exits 0 with VERIFIED + latency_ms < 50.
4. **Doctor:** `make doctor` all rows [OK].
5. **No Swift edits:** `git diff --name-only libs/cua-driver/Sources/` returns empty.
6. **All 9 plan SUMMARY.md files exist:** `ls .planning/phases/01-foundation-state-verifier/01-0[1-9]-SUMMARY.md` lists 9 files.
7. **PHASE-1-DEMO.md present and complete.**
</verification>

<success_criteria>
- VERIFY-04 satisfied (end-to-end): L0 push verifier wins on Calculator click in <50ms, no L2 escalation.
- VERIFY-05 satisfied (end-to-end): L1 cheap diff (CGWindowList + NSPasteboard.changeCount + dHash) supplements L0; total still <50ms.
- ROADMAP Phase 1 Success Criterion 1: Calculator click → kAXValueChanged → VERIFIED in <50ms via L0 push subscription. PROVEN by `test_under_50ms` and `run_demo()`.
- ROADMAP Phase 1 Success Criterion 2: State graph round-trip with composite key. PROVEN by `test_state_graph_roundtrip`.
- ROADMAP Phase 1 Success Criterion 3: AppProfile cache survives session restart. PROVEN by `test_appprofile_cache_persists` (and Plan 02's earlier test).
- ROADMAP Phase 1 Success Criterion 4: L0+L1 verifies in <50ms with no AX subtree walk. PROVEN by `test_l2_l3_not_invoked`.
- ROADMAP Phase 1 Success Criterion 5: AX rate-limit + depth-limited walker. PROVEN by `test_rate_limit.py` + `test_walker.py` (signature inspection in test_phase1_e2e SC-5).
- ROADMAP Phase 1 Success Criterion 6: trycua MCP surface preserved + healing wrapper exposed. PROVEN by `test_phase1_e2e SC-6` listing `click_with_healing` AND upstream tool.
- All 6 BLOCKER pitfalls (P2, P3, P14, P24, P25, P28) documented in PHASE-1-DEMO.md with mitigation references.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-state-verifier/01-09-SUMMARY.md` documenting:
- Demo invocation: `python -m cua_overlay.demo.calculator_click`
- run_demo() public surface (return dict shape)
- Measured latencies from test_under_50ms (median ms across 3 runs)
- 6 ROADMAP success criteria — PASS/FAIL grid
- 6 BLOCKER pitfalls — mitigation file:test reference grid
- PHASE-1-DEMO.md location for operator follow-up
- Phase 1 ship status (ready / not ready)
</output>
