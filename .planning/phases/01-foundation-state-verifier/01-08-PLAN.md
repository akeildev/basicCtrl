---
phase: 01-foundation-state-verifier
plan: 08
type: execute
wave: 4
depends_on: ["01-02", "01-04", "01-05", "01-06", "01-07"]
files_modified:
  - basicctrl/mcp_server/__init__.py
  - basicctrl/mcp_server/main.py
  - basicctrl/mcp_server/proxy.py
  - basicctrl/mcp_server/healing_tools.py
  - basicctrl/mcp_server/__main__.py
  - tests/integration/test_mcp_proxy.py
autonomous: true
requirements:
  - CORE-02
  - MCP-01
  - MCP-02
must_haves:
  truths:
    - "Python MCP server starts trycua's `cua-driver mcp` as a stdio subprocess on bootstrap"
    - "All upstream `cua-driver mcp` tools are mirrored into the proxy (list_tools returns both upstream tools AND new healing tools)"
    - "Proxy intercepts every action-class tool call with PRE-subscribe / DELEGATE / POST-aggregate"
    - "Healing tool `click_with_healing(x, y, bundle_id, pid, label)` is registered and callable via MCP protocol"
    - "Bind transport is stdio ONLY (T-1-01 mitigation: never TCP)"
    - "Existing trycua MCP tests pass when run against the proxy (regression test cited in 01-VALIDATION.md MCP-01 row)"
    - "On every action-class tool call, proxy records HoarePre + ActionCanonical + HoarePost via SessionWriter.append_action_log AND DurableExecutor.checkpoint"
  artifacts:
    - path: "basicctrl/mcp_server/main.py"
      provides: "FastMCP server bootstrap; spawns cua-driver mcp upstream; registers proxied + healing tools"
      contains: "FastMCP"
    - path: "basicctrl/mcp_server/proxy.py"
      provides: "Tool-mirroring logic + ACTION_CLASS_TOOLS table + verifier wrap"
      contains: "register_proxied_tool"
    - path: "basicctrl/mcp_server/healing_tools.py"
      provides: "click_with_healing tool"
      contains: "click_with_healing"
    - path: "basicctrl/mcp_server/__main__.py"
      provides: "Entry point: python -m basicctrl.mcp_server"
      contains: "asyncio.run"
  key_links:
    - from: "basicctrl/mcp_server/main.py"
      to: "mcp.client.stdio.stdio_client + StdioServerParameters(command='cua-driver', args=['mcp'])"
      via: "Subprocess spawn"
      pattern: "stdio_client"
    - from: "basicctrl/mcp_server/proxy.py"
      to: "basicctrl.verifier.Aggregator + AXObserverManager"
      via: "PRE/POST verifier wrap"
      pattern: "Aggregator"
    - from: "basicctrl/mcp_server/main.py"
      to: "basicctrl.persist.SessionWriter + DurableExecutor"
      via: "Per-session log file + Postgres checkpoint"
      pattern: "SessionWriter"
threat_model:
  trust_boundaries:
    - "T-1-01 (LOW): MCP server tool-call surface. Mitigated by binding stdio ONLY (no TCP); only locally-running clients (Claude Code, Cursor, Codex) can connect."
  threats:
    - id: T-1-01
      category: S
      component: basicctrl/mcp_server/main.py
      disposition: mitigate
      plan: "Use FastMCP.run_stdio_async() exclusively; never TCP. Acceptance criteria asserts no `bind`, `listen`, `socket.AF_INET` calls."
---

<objective>
Build the Python MCP server that PROXIES trycua's `cua-driver mcp` (Swift MCP server) and adds healing-wrapper tools.

Per ROADMAP success criterion 6: "Existing trycua MCP server surface still works; healing wrapper exposed as additional MCP tools so Claude Code / Cursor / Codex can invoke it."

Per CORE-02 (proxy approach from research Q1 resolution): we wrap `cua-driver mcp` over stdio. NO Swift edits.

Per MCP-01: "Maintain trycua/cua's existing MCP server surface."
Per MCP-02: "Expose self-healing wrapper as MCP tools."

Output:
- `basicctrl/mcp_server/` subpackage with main, proxy, healing_tools, __main__
- main.py wires AXEventBridge + AXObserverManager + L0+L1+L2+L3Stub + Aggregator + DurableExecutor + SessionWriter
- main.py spawns `cua-driver mcp` via stdio_client and mirrors tools
- proxy.py implements PRE-subscribe / DELEGATE / POST-aggregate per action-class tool
- healing_tools.py registers click_with_healing
- Integration test that lists tools and calls a healing tool
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-state-verifier/01-RESEARCH.md
@.planning/phases/01-foundation-state-verifier/01-VALIDATION.md
@CLAUDE.md

<source_files>
- ~/thinker/research-clones/trycua-cua/libs/python/mcp-server/mcp_server/server.py (FastMCP usage; lines 26, 118-170, 391-393 for `await server.run_stdio_async()`)
- ~/thinker/research-clones/trycua-cua/libs/cua-driver/Sources/CuaDriverCLI/CuaDriverCommand.swift (lines 302-344: MCPCommand uses StdioTransport — confirms `cua-driver mcp` is stdio MCP server)
- ~/thinker/research-clones/trycua-cua/libs/cua-driver/Sources/CuaDriverServer/ToolRegistry.swift (lines 34-45: actionToolNames {click, right_click, drag, scroll, type_text, type_text_chars, press_key, hotkey, set_value, page})
</source_files>

<interfaces>
ProxyDeps (basicctrl/mcp_server/main.py):
```python
@dataclass
class ProxyDeps:
    axmgr: AXObserverManager
    aggregator: Aggregator
    session: SessionWriter
    durable: DurableExecutor
```

ACTION_CLASS_TOOLS (basicctrl/mcp_server/proxy.py):
```python
ACTION_CLASS_TOOLS: dict[str, str] = {
    "click": "click", "right_click": "click", "drag": "click",
    "scroll": "scroll", "page": "scroll",
    "type_text": "type", "type_text_chars": "type", "press_key": "type", "hotkey": "type",
    "set_value": "set_value",
}
```

main() coroutine:
```python
async def main() -> None:
    """Bootstrap. Sets up Phase 1 stack, spawns cua-driver mcp, registers proxied + healing tools, runs FastMCP.run_stdio_async()."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap main.py - spawn cua-driver mcp + wire all dependencies</name>
  <read_first>
    - ~/thinker/research-clones/trycua-cua/libs/python/mcp-server/mcp_server/server.py (FastMCP setup)
    - ~/thinker/research-clones/trycua-cua/libs/cua-driver/Sources/CuaDriverCLI/CuaDriverCommand.swift (confirm cua-driver mcp uses StdioTransport)
    - basicctrl/verifier/__init__.py (Plan 04-06 exports)
    - basicctrl/persist/__init__.py (Plan 07 exports)
    - basicctrl/log.py (structlog setup)
  </read_first>
  <files>basicctrl/mcp_server/__init__.py, basicctrl/mcp_server/main.py, basicctrl/mcp_server/__main__.py</files>
  <behavior>
    - Test 1 (manual smoke): `uv run python -c "from basicctrl.mcp_server import main, ProxyDeps"` exits 0.
    - Test 2 (manual smoke): `uv run python -c "import basicctrl.mcp_server.__main__"` exits 0.
    - Test 3 (Task 3 integration): When server runs as subprocess, MCP client can connect via stdio and list tools.
  </behavior>
  <action>
1. **Create `basicctrl/mcp_server/__init__.py`**:
   ```python
   from basicctrl.mcp_server.main import main, ProxyDeps
   __all__ = ["main", "ProxyDeps"]
   ```

2. **Create `basicctrl/mcp_server/main.py`** with the full content:
   - Import asyncio, os, structlog, FastMCP, ClientSession, StdioServerParameters, stdio_client
   - Import from basicctrl.log: configure as configure_logging
   - Import from basicctrl.ax.observer: AXEventBridge
   - Import from basicctrl.verifier: AXObserverManager, NSWorkspaceObserver, KqueueProcObserver, L0Push, L1Cheap, L2Medium, L3Stub, WeightedVote, Aggregator
   - Import from basicctrl.persist: SessionWriter, DurableExecutor
   - Define `@dataclass class ProxyDeps` with fields: axmgr (AXObserverManager), aggregator (Aggregator), session (SessionWriter), durable (DurableExecutor)
   - Define `async def main() -> None`:
     1. `configure_logging()`; bind a structlog logger
     2. `loop = asyncio.get_running_loop()`
     3. `bridge = AXEventBridge(loop=loop); bridge.start()`
     4. `axmgr = AXObserverManager(bridge=bridge); axmgr.start()`
     5. `ws = NSWorkspaceObserver(loop=loop); ws.start()`
     6. `async with KqueueProcObserver(loop=loop) as kq:` ... continue inside the with block
     7. Inside: build verifier ensemble: `l0 = L0Push(axmgr=axmgr, ws=ws, kq=kq); l1 = L1Cheap(); l2 = L2Medium(); l3 = L3Stub(); vote = WeightedVote(); aggregator = Aggregator(l0=l0, l1=l1, l2=l2, l3=l3, vote=vote)`
     8. Persistence: `session = SessionWriter(); durable = DurableExecutor()`. Try `await durable.setup()` inside try/except — if it raises, log warning `durable.setup_failed_continuing_without_postgres` with the connection-hint string and continue (Plan 07's init_postgres.sh is the recovery path).
     9. `deps = ProxyDeps(axmgr=axmgr, aggregator=aggregator, session=session, durable=durable)`
     10. Build `upstream_params = StdioServerParameters(command=os.environ.get("CUA_DRIVER_BIN", "cua-driver"), args=["mcp"], env=None)`
     11. `proxy_server = FastMCP(name="basicCtrl")`
     12. `try: async with stdio_client(upstream_params) as (read, write): async with ClientSession(read, write) as upstream: await upstream.initialize(); upstream_tools = await upstream.list_tools()` — log `upstream.connected` with tool count
     13. Mirror upstream tools: `from basicctrl.mcp_server.proxy import register_proxied_tool; for tool in upstream_tools.tools: await register_proxied_tool(proxy_server, upstream, tool, deps)`
     14. Register healing tools: `from basicctrl.mcp_server.healing_tools import register_healing_tools; await register_healing_tools(proxy_server, upstream, deps)`
     15. Log `proxy.ready` with tool names + session_id
     16. `await proxy_server.run_stdio_async()`
     17. `except FileNotFoundError as e:` — log error `upstream.cua_driver_not_found` with hint to build cua-driver, then re-raise
     18. `finally:` — `await axmgr.stop(); bridge.stop(); ws.stop(); await durable.aclose()`

3. **Create `basicctrl/mcp_server/__main__.py`**:
   ```python
   import asyncio
   from basicctrl.mcp_server.main import main

   if __name__ == "__main__":
       asyncio.run(main())
   ```

4. **Verify:** `uv run python -c "from basicctrl.mcp_server import main, ProxyDeps"` exits 0.
  </action>
  <verify>
    <automated>uv run python -c "from basicctrl.mcp_server import main, ProxyDeps; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `basicctrl/mcp_server/main.py` and `grep -c "FastMCP" basicctrl/mcp_server/main.py` returns at least 1.
    - `grep -c "stdio_client" basicctrl/mcp_server/main.py` returns at least 1.
    - `grep -c "StdioServerParameters" basicctrl/mcp_server/main.py` returns at least 1.
    - `grep -c "run_stdio_async" basicctrl/mcp_server/main.py` returns at least 1.
    - `grep -c "AXEventBridge\\|AXObserverManager" basicctrl/mcp_server/main.py` returns at least 2.
    - `grep -c "Aggregator" basicctrl/mcp_server/main.py` returns at least 1.
    - `grep -c "SessionWriter\\|DurableExecutor" basicctrl/mcp_server/main.py` returns at least 2.
    - `grep -E "args=\\[\"mcp\"\\]" basicctrl/mcp_server/main.py` returns at least 1.
    - `grep -E "socket\\.AF_INET|listen\\(|\\.bind\\(" basicctrl/mcp_server/main.py` returns 0 matches (T-1-01: stdio only).
    - `grep -c "configure_logging\\|configure()" basicctrl/mcp_server/main.py` returns at least 1.
    - Public API import: `uv run python -c "from basicctrl.mcp_server import main, ProxyDeps"` exits 0.
    - Module entry runnable: `uv run python -c "import basicctrl.mcp_server.__main__"` exits 0.
  </acceptance_criteria>
  <done>main.py wires all Phase 1 dependencies, spawns `cua-driver mcp` via stdio_client, runs FastMCP on stdio. T-1-01 mitigated (no TCP). Module entry point available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proxy logic + healing tools (PRE-subscribe / DELEGATE / POST-aggregate)</name>
  <read_first>
    - ~/thinker/research-clones/trycua-cua/libs/cua-driver/Sources/CuaDriverServer/ToolRegistry.swift (actionToolNames lines 34-45)
    - .planning/phases/01-foundation-state-verifier/01-RESEARCH.md (sections "ToolRegistry Hook Strategy", "MCP Proxy")
    - basicctrl/verifier/__init__.py (Aggregator, AXObserverManager)
    - basicctrl/persist/__init__.py (SessionWriter, DurableExecutor)
    - basicctrl/state/causal_dag.py (HoarePre, HoarePost, ActionCanonical)
    - basicctrl/mcp_server/main.py (Task 1; ProxyDeps signature)
  </read_first>
  <files>basicctrl/mcp_server/proxy.py, basicctrl/mcp_server/healing_tools.py</files>
  <behavior>
    - Behavior 1: action-class tool ("click") wrap captures L1 snapshot before delegate, calls aggregator.verify after delegate, writes one action_log line + one Postgres row.
    - Behavior 2: non-action tool ("screenshot") is straight passthrough — no aggregator.verify call.
    - Behavior 3: healing tool click_with_healing is callable; returns dict containing session_id + phase=1 marker.
  </behavior>
  <action>
1. **Create `basicctrl/mcp_server/proxy.py`** with:
   - Imports: time, uuid, datetime+timezone, Any, Optional, structlog, FastMCP, ClientSession, mcp.types.Tool
   - Imports from basicctrl.state.causal_dag: HoarePre, HoarePost, ActionCanonical
   - Imports from basicctrl.state.graph: UIElement, Bbox
   - Imports from basicctrl.verifier.ensemble.l1_cheap: L1Cheap
   - Imports from basicctrl.mcp_server.main: ProxyDeps
   - Define `ACTION_CLASS_TOOLS: dict[str, str]` with EXACT mapping (verbatim from interfaces block above)
   - Define `_step_counter = {"value": 0}` (module-level monotonic counter)
   - Define `async def register_proxied_tool(proxy: FastMCP, upstream: ClientSession, tool: Tool, deps: ProxyDeps) -> None`:
     - Check if `tool.name in ACTION_CLASS_TOOLS`. If NOT → register a passthrough function that just calls `await upstream.call_tool(tool.name, arguments=kwargs)` and returns result.content; log `registered.passthrough`.
     - If YES → register a wrapped function:
       1. Generate `action_id = uuid.uuid4().hex`; increment `_step_counter["value"]`; record `t_start = time.monotonic()`
       2. Build minimal target via `_build_minimal_target(kwargs, deps.session.session_id)`
       3. Build HoarePre with target_key, target_exists=True, target_enabled=True, target_role=target.role, role_compatible=True, frontmost_app from kwargs.get("bundle_id", ""), no_blocking_modal=True, timestamp_ns=time.monotonic_ns()
       4. Capture L1 baseline: `l1_before = await L1Cheap().snapshot(target)`
       5. FIRE: `result = await upstream.call_tool(tool.name, arguments=kwargs)`
       6. Build ActionCanonical with id=action_id, step_idx, kind="MUTATE", target_key, action_type=ACTION_CLASS_TOOLS[tool.name], payload=dict(kwargs), tier=None, channel=None, timestamp_ns=time.monotonic_ns(), session_id=deps.session.session_id
       7. POST: `post = await deps.aggregator.verify(action=action, target=target, notifs=["AXValueChanged", "AXFocusedUIElementChanged"], before_l1=l1_before, ax_element=None, timeout_ms=50)`
       8. LOG: `deps.session.append_action_log({...})` with step_idx, action_id, tool, action_type, pre.model_dump, action.model_dump, post.model_dump, elapsed_ms
       9. CHECKPOINT: `try: await deps.durable.checkpoint(session_id=..., step_idx=..., pre=pre, action=action, post=post) except Exception as e: log.warning("durable.checkpoint_failed", error=str(e))` — non-fatal so the MCP call still returns.
       10. Return `result.content if hasattr(result, "content") else result`
     - Log `registered.action_class` with action_class
   - Define `def _build_minimal_target(kwargs: dict, session_id: str) -> UIElement` returning UIElement with role="AXButton", role_path="AXApplication/AXButton[?]", label=str(kwargs.get("label", "")), bbox=Bbox(x=float(kwargs.get("x", 0)), y=float(kwargs.get("y", 0)), w=20.0, h=20.0), discovered_at=now, last_seen_at=now, pid=int(kwargs.get("pid", 0)), bundle_id=str(kwargs.get("bundle_id", "")), window_id=int(kwargs.get("window_id", 0)).

2. **Create `basicctrl/mcp_server/healing_tools.py`**:
   - Imports: structlog, FastMCP, ClientSession, ProxyDeps
   - `async def register_healing_tools(proxy: FastMCP, upstream: ClientSession, deps: ProxyDeps) -> None`:
     - Register `click_with_healing(x: int, y: int, bundle_id: str = "", pid: int = 0, label: str = "") -> dict` as a FastMCP tool with description "Click on a target element with self-healing recovery. Phase 1: same as upstream click + verifier wrap. Phase 3 will add 5-branch parallel recovery."
     - Body: `result = await upstream.call_tool("click", arguments={"x": x, "y": y, "bundle_id": bundle_id, "pid": pid, "label": label})`
     - Return `{"result": result.content if hasattr(result, "content") else str(result), "session_id": deps.session.session_id, "phase": 1, "note": "Phase 1 wrapper; Phase 3 will add 5-branch recovery + cache write-back"}`
     - Log `healing_tools.registered` with `tools=["click_with_healing"]`

3. **Verify imports succeed:** `uv run python -c "from basicctrl.mcp_server.proxy import register_proxied_tool, ACTION_CLASS_TOOLS; from basicctrl.mcp_server.healing_tools import register_healing_tools; assert 'click' in ACTION_CLASS_TOOLS"` exits 0.
  </action>
  <verify>
    <automated>uv run python -c "from basicctrl.mcp_server.proxy import register_proxied_tool, ACTION_CLASS_TOOLS; from basicctrl.mcp_server.healing_tools import register_healing_tools; assert 'click' in ACTION_CLASS_TOOLS and 'type_text' in ACTION_CLASS_TOOLS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `basicctrl/mcp_server/proxy.py` and `grep -c "ACTION_CLASS_TOOLS" basicctrl/mcp_server/proxy.py` returns at least 2.
    - `grep -c "\"click\"\\|\"type_text\"\\|\"scroll\"" basicctrl/mcp_server/proxy.py` returns at least 6 (multiple tool name string literals in the dict).
    - `grep -c "L1Cheap\\(\\)\\.snapshot\\|L1Cheap()" basicctrl/mcp_server/proxy.py` returns at least 1 (PRE snapshot).
    - `grep -c "deps\\.aggregator\\.verify\\|aggregator\\.verify" basicctrl/mcp_server/proxy.py` returns at least 1 (POST aggregate).
    - `grep -c "deps\\.session\\.append_action_log\\|append_action_log" basicctrl/mcp_server/proxy.py` returns at least 1.
    - `grep -c "deps\\.durable\\.checkpoint\\|durable\\.checkpoint" basicctrl/mcp_server/proxy.py` returns at least 1.
    - `grep -c "ActionCanonical" basicctrl/mcp_server/proxy.py` returns at least 1.
    - File exists: `basicctrl/mcp_server/healing_tools.py` and `grep -c "click_with_healing" basicctrl/mcp_server/healing_tools.py` returns at least 1.
    - `grep -c "@proxy.tool" basicctrl/mcp_server/healing_tools.py` returns at least 1.
    - Public API: `uv run python -c "from basicctrl.mcp_server.proxy import register_proxied_tool, ACTION_CLASS_TOOLS; assert 'click' in ACTION_CLASS_TOOLS and 'type_text' in ACTION_CLASS_TOOLS"` exits 0.
  </acceptance_criteria>
  <done>register_proxied_tool wraps action-class tools with PRE-subscribe / DELEGATE / POST-aggregate; passes through non-action tools. healing_tools.py registers click_with_healing. ACTION_CLASS_TOOLS table covers all of trycua's actionToolNames.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration test — list tools + call healing tool against Calculator</name>
  <read_first>
    - .planning/phases/01-foundation-state-verifier/01-RESEARCH.md (section "Smoke test" subsection of MCP Proxy)
    - .planning/phases/01-foundation-state-verifier/01-VALIDATION.md (rows for CORE-02, MCP-01, MCP-02)
    - basicctrl/mcp_server/main.py (Task 1)
    - tests/conftest.py (calculator_pid fixture from Plan 01)
  </read_first>
  <files>tests/integration/test_mcp_proxy.py</files>
  <behavior>
    - Test 1 (test_mcp_proxy.py::test_list_tools): Spawn `uv run python -m basicctrl.mcp_server` as subprocess via stdio_client. Connect MCP ClientSession; await initialize(); await list_tools(). Assert returned tool list contains `click_with_healing` AND at least one upstream tool name (e.g., `click` from cua-driver).
    - Test 2 (test_mcp_proxy.py::test_screenshot_passthrough): Call upstream tool `screenshot_cua` (or whatever the upstream screenshot is) via the proxy. Assert returned content has non-zero size (PNG bytes or base64).
    - Test 3 (test_mcp_proxy.py::test_healing_tool_callable): Launch Calculator via fixture. Call `click_with_healing(x=cx, y=cy, bundle_id="com.apple.Calculator", pid=cal_pid)` where (cx, cy) is the "5" button center. Assert returned dict has `phase == 1` and `session_id` is a UUID-shaped string.
    - Test 4 (test_mcp_proxy.py::test_action_log_written): After calling click_with_healing, find the session_id in returned dict; assert `~/.cua/sessions/<session_id>/action_log.ndjson` exists and contains at least one valid JSON line.
  </behavior>
  <action>
1. **Create `tests/integration/test_mcp_proxy.py`** with all 4 tests. Each test marked `@pytest.mark.integration`. Each test has its own MCP client subprocess (don't share state).

2. The test pattern for spawning the proxy:
   ```python
   import asyncio, subprocess, sys
   from mcp.client.session import ClientSession
   from mcp.client.stdio import StdioServerParameters, stdio_client

   async def _connect():
       params = StdioServerParameters(
           command=sys.executable,
           args=["-m", "basicctrl.mcp_server"],
           env=None,
       )
       async with stdio_client(params) as (read, write):
           async with ClientSession(read, write) as session:
               await session.initialize()
               yield session
   ```

3. For Test 3 (Calculator click), the test must:
   - Use `calculator_pid` fixture
   - Resolve "5" button center: read AppProfile for Calculator, run a quick walker to find a button labeled "5", or hard-code a known button position from a reference screenshot. Use the explicit known-good coordinates approach: Calculator's "5" button on default macOS 26 layout is reliably around (200, 540) when launched at default position — but to be robust, do a one-shot AX scan via `basicctrl.ax.walker.walk_subtree` to locate it.

4. For Test 4 (action_log written), after the click:
   - Read the returned `session_id`
   - Wait briefly for filesystem flush (`await asyncio.sleep(0.1)`)
   - `assert (Path.home() / ".cua" / "sessions" / session_id / "action_log.ndjson").exists()`
   - Open the file, read all lines, parse each as JSON, assert at least one has `tool == "click"` AND `action_type == "click"`.

5. **Verify (with cua-driver built and on PATH OR with CUA_DRIVER_BIN env var set):**
   - `uv run pytest -x -v -m integration tests/integration/test_mcp_proxy.py` exits 0 with at least 4 PASSED.
   - If `cua-driver` binary isn't available, tests skip with clear `pytest.skip("cua-driver not on PATH")` message.

6. **Document in test docstring:**
   - "Requires `cua-driver mcp` available on PATH or via `CUA_DRIVER_BIN` env var."
   - "Build: `cd libs/cua-driver && swift build -c release`."
  </action>
  <verify>
    <automated>uv run pytest -x -q -m integration tests/integration/test_mcp_proxy.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `tests/integration/test_mcp_proxy.py`.
    - `grep -c "test_list_tools\\|test_screenshot_passthrough\\|test_healing_tool_callable\\|test_action_log_written" tests/integration/test_mcp_proxy.py` returns at least 4.
    - `grep -c "stdio_client\\|StdioServerParameters" tests/integration/test_mcp_proxy.py` returns at least 1.
    - `grep -c "click_with_healing" tests/integration/test_mcp_proxy.py` returns at least 2.
    - `grep -c "action_log.ndjson" tests/integration/test_mcp_proxy.py` returns at least 1.
    - `grep -c "@pytest.mark.integration" tests/integration/test_mcp_proxy.py` returns at least 4.
    - With cua-driver built + Calculator launchable: `uv run pytest -x -q -m integration tests/integration/test_mcp_proxy.py` exits 0 with at least 4 PASSED (or skipped with clear reason if cua-driver missing).
  </acceptance_criteria>
  <done>End-to-end MCP proxy integration test: spawns the Python MCP server, lists tools (proxied + healing), calls click_with_healing against Calculator, verifies action_log.ndjson is appended. CORE-02, MCP-01, MCP-02 satisfied.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | S (Spoofing) | basicctrl/mcp_server/main.py | mitigate | FastMCP.run_stdio_async() exclusively; never TCP. Acceptance criteria asserts no socket.AF_INET or .bind() or listen() calls in the codebase. |
</threat_model>

<verification>
1. **Module import smoke:** `uv run python -c "from basicctrl.mcp_server import main, ProxyDeps; from basicctrl.mcp_server.proxy import ACTION_CLASS_TOOLS; from basicctrl.mcp_server.healing_tools import register_healing_tools"` exits 0.
2. **Module entry runnable:** `uv run python -c "import basicctrl.mcp_server.__main__"` exits 0.
3. **No TCP transport:** `grep -E "socket\\.AF_INET|listen\\(|\\.bind\\(" basicctrl/mcp_server/` returns 0 matches recursively.
4. **Integration tests (with cua-driver built + Calculator):** `uv run pytest -x -q -m integration tests/integration/test_mcp_proxy.py` exits 0.
5. **Upstream regression test:** `cd libs/python/mcp-server && uv run pytest -x` (trycua's existing tests) — regression check, not strictly required to pass for Phase 1 but confirms upstream unmodified.
</verification>

<success_criteria>
- CORE-02 satisfied: ToolRegistry post-action callbacks intercepted via MCP proxy approach (PRE-subscribe + POST-aggregate) without editing Swift.
- MCP-01 satisfied: trycua's MCP tool surface preserved via passthrough + wrap (depending on whether tool is action-class or not).
- MCP-02 satisfied: click_with_healing tool exposed; Phase 3 will swap body for 5-branch recovery.
- T-1-01 mitigated: stdio-only transport; no TCP.
- Every action-class tool call produces: HoarePre + ActionCanonical + HoarePost recorded to action_log.ndjson AND Postgres checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-state-verifier/01-08-SUMMARY.md` documenting:
- main.py bootstrap sequence (verbatim ordering)
- ACTION_CLASS_TOOLS mapping (verbatim)
- Per-action wrap order (PRE: L1 snapshot + AX expect via aggregator; FIRE: upstream.call_tool; POST: aggregator.verify; LOG: action_log + Postgres checkpoint)
- Healing tool surface (click_with_healing signature + return dict shape)
- T-1-01 mitigation note (stdio only)
- One-time setup: build cua-driver, run init_postgres.sh, set CUA_DRIVER_BIN if needed
</output>
